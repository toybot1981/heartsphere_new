## 1. 数据库迁移脚本框架

- [x] 1.1 创建主迁移脚本 `deploy/migrate-database.sh`
  - 支持本地和生产环境的数据库连接配置
  - 实现参数解析（环境选择、干运行模式等）
  - 添加日志输出和错误处理

- [x] 1.2 创建数据库结构对比工具
  - 实现 `deploy/scripts/compare-schema.sh`
  - 导出本地和远程数据库的表结构
  - 对比表、字段、索引的差异
  - 生成差异报告

- [x] 1.3 创建表结构同步脚本
  - 实现 `deploy/scripts/sync-schema.sh`
  - 创建缺失的表（基于本地表结构）
  - 添加缺失的字段
  - 更新索引（仅添加，不删除）

- [x] 1.4 创建数据同步脚本
  - 实现 `deploy/scripts/sync-data.sh`
  - 识别系统数据表（system_* 前缀的表）
  - 系统数据完全同步（INSERT ... ON DUPLICATE KEY UPDATE）
  - 用户数据部分同步（仅更新图片相关字段）

## 2. 配置和文档

- [x] 2.1 创建数据库连接配置文件
  - `deploy/db-migration.config` - 包含本地和生产环境的数据库连接信息
  - 支持环境变量覆盖
  - 添加敏感信息保护（密码不写入配置文件，使用环境变量或提示输入）

- [x] 2.2 创建迁移文档
  - `deploy/DATABASE_MIGRATION_GUIDE.md` - 迁移使用指南
  - 说明迁移流程、注意事项、故障处理

- [x] 2.3 添加备份机制
  - 在迁移前自动备份远程数据库
  - 提供备份恢复脚本

## 3. 测试和验证

- [x] 3.1 添加干运行模式
  - 支持 `--dry-run` 参数，仅显示将要执行的操作，不实际执行

- [x] 3.2 添加验证脚本
  - 迁移后验证表结构一致性
  - 验证关键系统数据完整性

- [ ] 3.3 测试迁移脚本
  - 在测试环境验证迁移流程
  - 验证表结构同步功能
  - 验证数据同步功能
