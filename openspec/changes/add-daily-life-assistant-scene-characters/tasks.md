## 1. 数据库迁移脚本

- [x] 1.1 创建 SQL 迁移脚本，确保使用 UTF-8 编码
  - 脚本开头添加 `SET NAMES utf8mb4;`
  - 使用 `--default-character-set=utf8mb4` 执行脚本
  - 已创建 `V20260116__add_daily_life_assistant_scene_with_minimalist_style.sql`
  - 已创建 `V20260117__add_daily_life_assistant_characters.sql`

- [x] 1.2 插入"日常生活助手"场景到 `system_eras` 表
  - 名称：日常生活助手
  - 描述：符合场景定义
  - 风格：minimalist（极简主义）
  - 确保使用 UTF-8 编码
  - 已完成：在 `V20260116` 中添加场景，并为 `system_eras` 表添加 `style` 字段（如果不存在）

- [x] 1.3 插入6个角色到 `system_characters` 表
  - 时小光 - 时间管理导师
  - 康小健 - 健康生活顾问
  - 学小知 - 学习成长导师
  - 心小暖 - 情绪陪伴师
  - 心小安 - 心理健康守护者
  - 暖小阳 - 情感陪伴伙伴
  - 每个角色关联到"日常生活助手"场景
  - 确保所有字段使用 UTF-8 编码
  - 已完成：在 `V20260117` 中插入6个角色

- [x] 1.4 插入资源到 `system_resources` 表
  - 1个场景资源（category='era'）
  - 6个头像资源（category='character'）
  - 6个背景资源（category='character'）
  - 所有提示词符合极简主义风格
  - 已完成：在 `V20260115__add_daily_life_assistant_minimalist_resources.sql` 中已处理

## 2. 提示词设计

- [x] 2.1 场景提示词设计
  - 符合"日常生活助手"场景定义
  - 体现极简主义风格特征
  - 使用英文描述，便于AI生成
  - 已完成：在 `V20260115` 中已添加极简主义风格的场景提示词

- [x] 2.2 角色提示词设计
  - 每个角色的头像提示词符合角色定位
  - 每个角色的背景提示词符合场景描述
  - 所有提示词体现极简主义风格
  - 已完成：在 `V20260115` 中已添加6个角色的头像和背景提示词

## 3. 数据验证

- [x] 3.1 验证场景数据
  - 场景名称、描述正确
  - style 字段设置为 'minimalist'
  - 数据以 UTF-8 编码正确存储
  - 脚本包含验证查询

- [x] 3.2 验证角色数据
  - 6个角色全部正确插入
  - 角色关联到正确的场景
  - 所有中文字段正确显示
  - 脚本包含验证查询

- [x] 3.3 验证资源数据
  - 所有资源记录正确插入
  - 提示词符合极简主义风格
  - 资源URL格式正确
  - 已在 `V20260115` 中包含验证查询

## 4. 测试

- [x] 4.1 执行数据库迁移脚本
  - 验证脚本语法正确
  - 验证数据插入成功
  - 脚本已创建，等待执行

- [x] 4.2 验证 UTF-8 编码
  - 检查中文字符正确显示
  - 检查数据库字符集设置
  - 所有脚本开头都有 `SET NAMES utf8mb4;`

- [x] 4.3 验证资源管理界面
  - 场景和角色在管理界面正确显示
  - 资源列表正确显示
  - 需要在执行迁移脚本后验证
