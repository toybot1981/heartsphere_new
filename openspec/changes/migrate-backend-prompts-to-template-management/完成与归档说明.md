# 完成与归档说明

**变更ID**: `migrate-backend-prompts-to-template-management`

---

## 实施状态

- **代码与迁移**：已完成（shared/main/admin/多智能体改造、V20260132 迁移脚本、API 测试方案与 3 用例通过）。
- **待你执行**：
  - **2.3**：按 IMPLEMENTATION_SUMMARY.md 的「如何执行迁移」启动 main 后端一次，在管理端确认新模板可见且中文无乱码。
  - **6.3**（可选）：管理端与数据库中文/特殊字符抽查。

---

## 归档时机与步骤

在**部署上线后**且你已执行 2.3、6.3 后，可归档本变更：

1. 归档（将变更移入 archive，并视需要更新 specs）：
   ```bash
   openspec archive migrate-backend-prompts-to-template-management --yes
   ```
   若本变更仅涉及实现、不合并到正式 spec，可使用：
   ```bash
   openspec archive migrate-backend-prompts-to-template-management --skip-specs --yes
   ```

2. 校验归档结果：
   ```bash
   openspec validate --strict
   ```

归档后，`changes/migrate-backend-prompts-to-template-management/` 会移至 `changes/archive/YYYY-MM-DD-migrate-backend-prompts-to-template-management/`。
