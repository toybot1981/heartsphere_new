## 1. 创建场景创建向导组件结构

- [x] 1.1 创建 `SceneCreationWizard.tsx` 主组件
  - [x] 定义组件接口（props: token, worldId, onComplete, onCancel）
  - [x] 实现步骤状态管理（step: 1-4）
  - [x] 实现加载状态管理
  - [x] 实现数据状态管理（选中的场景、角色、主线剧情、剧本）

- [x] 1.2 创建场景选择步骤组件 `SceneSelectionStep.tsx`
  - [x] 显示预置场景列表（使用 `LazyImage` 显示场景图片，使用 `thumbnail` 分辨率）
  - [x] 实现多选功能（checkbox）
  - [x] 实现自定义名称输入（手动输入）
  - [x] 实现AI生成名称功能
  - [x] 实现场景选择/取消逻辑

- [x] 1.3 创建角色选择步骤组件 `CharacterSelectionStep.tsx`
  - [x] 按场景分组显示角色列表
  - [x] 实现多选功能（checkbox）
  - [x] 实现自定义名称输入（手动输入）
  - [x] 实现AI生成名称功能
  - [x] 实现角色选择/取消逻辑

- [x] 1.4 创建主线剧情选择步骤组件 `MainStorySelectionStep.tsx`
  - [x] 按场景分组显示主线剧情
  - [x] 实现单选功能（每个场景最多选择一个）
  - [x] 实现自定义名称输入（手动输入）
  - [x] 实现AI生成名称功能
  - [x] 实现主线剧情选择/取消逻辑

- [x] 1.5 创建剧本选择步骤组件 `ScriptSelectionStep.tsx`
  - [x] 按场景分组显示剧本列表
  - [x] 实现多选功能（checkbox）
  - [x] 实现自定义名称输入（手动输入）
  - [x] 实现AI生成名称功能
  - [x] 实现剧本选择/取消逻辑

- [ ] 1.6 创建创建摘要预览组件 `SceneCreationSummary.tsx`（可选）
  - [ ] 显示即将创建的场景、角色、主线剧情、剧本数量
  - [ ] 显示创建摘要信息

## 2. 实现数据加载逻辑

- [x] 2.1 实现预置场景加载
  - [x] 调用 `eraApi.getSystemEras()` 加载预置场景列表
  - [x] 处理加载错误

- [x] 2.2 实现角色加载逻辑
  - [x] 根据选中的场景，调用 `characterApi.getSystemCharacters(eraId)` 加载角色
  - [x] 按场景ID分组存储角色数据
  - [x] 处理加载错误（某些场景可能没有角色）

- [x] 2.3 实现主线剧情加载逻辑
  - [ ] 根据选中的场景，调用 `presetMainStoryApi.getByEraId(eraId)` 加载主线剧情
  - [x] 按场景ID分组存储主线剧情数据
  - [x] 处理加载错误（某些场景可能没有主线剧情）

- [x] 2.4 实现剧本加载逻辑
  - [x] 根据选中的场景，调用 `presetScriptApi.getByEraId(eraId)` 加载剧本
  - [x] 按场景ID分组存储剧本数据
  - [x] 处理加载错误（某些场景可能没有剧本）

## 3. 实现批量创建逻辑

- [x] 3.1 实现场景批量创建
  - [x] 遍历选中的场景，调用 `eraApi.createEra()` 创建场景
  - [x] 保存原场景ID到新场景ID的映射关系
  - [x] 处理创建错误（部分成功的情况）

- [x] 3.2 实现角色批量创建
  - [x] 遍历选中的角色，根据场景映射关系确定目标场景ID
  - [x] 调用 `characterApi.createCharacter()` 创建角色
  - [x] 处理创建错误

- [x] 3.3 实现主线剧情批量创建
  - [x] 遍历选中的主线剧情，根据场景映射关系确定目标场景ID
  - [x] 调用 `userMainStoryApi.create()` 创建主线剧情
  - [x] 处理创建错误

- [x] 3.4 实现剧本批量创建
  - [x] 遍历选中的剧本，根据场景映射关系确定目标场景ID
  - [x] 调用 `scriptApi.createScript()` 创建剧本
  - [x] 处理创建错误

- [x] 3.5 实现创建进度提示
  - [x] 显示创建进度（正在创建场景、角色、主线剧情、剧本）
  - [x] 显示创建结果（成功/失败数量）

## 4. 集成到场景列表页面

- [x] 4.1 在 `SceneSelectionScreen.tsx` 中添加"批量创建场景"按钮
  - [x] 添加按钮UI（符合UX设计规范）
  - [x] 实现打开向导的逻辑
  - [x] 实现创建完成后的刷新逻辑

- [x] 4.2 在 `EraConstructorModal.tsx` 中添加"使用向导创建"选项（可选）
  - [x] 添加选项按钮或链接
  - [x] 实现打开向导的逻辑
  - [x] 关闭当前模态框

## 5. 移动端支持（可选）

- [ ] 5.1 在 `MobileSceneSelectionScreen.tsx` 中添加批量创建入口
  - [ ] 添加按钮UI（符合移动端UX设计规范）
  - [ ] 实现打开向导的逻辑（可以使用PC版本的向导，或创建移动端专用版本）

## 6. 错误处理和用户体验优化

- [x] 6.1 实现错误处理
  - [x] 网络错误处理
  - [x] API错误处理
  - [x] 部分创建失败的处理（显示哪些成功、哪些失败）

- [x] 6.2 实现加载状态显示
  - [x] 数据加载时的loading状态
  - [x] 创建过程中的进度提示

- [x] 6.3 实现表单验证
  - [x] 至少选择一个场景
  - [x] 自定义名称不能为空（如果用户修改了名称）

- [x] 6.4 实现UX优化
  - [x] 步骤导航（上一步/下一步按钮）
  - [x] 步骤指示器（显示当前步骤）
  - [ ] 键盘导航支持（可选）
  - [x] 响应式布局（适配不同屏幕尺寸）

## 7. 测试

- [ ] 7.1 单元测试
  - [ ] 测试各个步骤组件的交互逻辑
  - [ ] 测试数据加载逻辑
  - [ ] 测试批量创建逻辑

- [ ] 7.2 集成测试
  - [ ] 测试完整的创建流程
  - [ ] 验证数据正确创建到数据库
  - [ ] 验证场景、角色、主线剧情、剧本的关联关系正确

- [ ] 7.3 UI测试
  - [ ] 验证向导界面在不同屏幕尺寸下的显示效果
  - [ ] 验证图片显示使用正确的分辨率（thumbnail）
  - [ ] 验证交互流程符合UX设计规范

- [ ] 7.4 错误处理测试
  - [ ] 测试网络错误情况
  - [ ] 测试API错误情况
  - [ ] 测试部分创建失败的情况

## 8. 文档和代码审查

- [ ] 8.1 代码审查
  - [ ] 确保代码符合项目规范
  - [ ] 确保遵循UX设计规范
  - [ ] 确保图片使用正确的分辨率规则

- [ ] 8.2 更新相关文档（如有需要）
  - [ ] 更新用户指南（如有）
  - [ ] 更新开发文档（如有）
