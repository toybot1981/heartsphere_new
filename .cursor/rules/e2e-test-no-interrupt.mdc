---
description: 运行 E2E/技能管理自动化测试时确保测试不被打断
globs: 
  - admin/e2e/**/*
  - .claude/skills/web-automation-testing/**/*
---

# E2E 自动化测试：不打断

无论何种情况，请确保自动化测试**不会被打断**。

- 当运行 **test_runner.py** 或 **test_executor.py**（技能管理或任意 web-automation-testing 测试）时，必须为**执行命令**设置**至少 30 分钟**超时（`timeout=1800000`，单位毫秒），否则执行环境可能在中途终止进程，导致测试未跑完即退出。
- 脚本内部不对整次测试设置总超时；仅单步有超时（防止某一步无限卡住）。
- 若在本机终端直接执行上述命令，则无执行环境超时，可完整跑完。
