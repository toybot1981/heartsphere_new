# 计费模块使用指南

## 📖 概述

计费模块是一个基于AOP切面的非侵入式计费系统，自动拦截AI服务调用，实现基于Token的精确计费。系统支持文本生成、图片生成、音频处理、视频生成等多种AI服务的计费。

## 🎯 核心特性

1. **非侵入式设计**：通过注解自动计费，无需修改业务代码
2. **自动配额检查**：调用前自动检查用户配额和资源池余额
3. **精确计费**：基于实际使用量（Token、图片数等）计算费用
4. **资源池管理**：支持供应商级别的资源池管理和自动充值提醒
5. **使用记录**：完整记录每次AI调用的详细信息
6. **实时统计**：提供用户实时使用统计和配额监控

## 🚀 快速开始

### 1. 基本使用

在AI服务方法上添加 `@RequiresTokenQuota` 注解即可自动计费：

```java
@Service
public class AIServiceImpl implements AIService {
    
    @RequiresTokenQuota(quotaType = "text_token", usageType = "text_generation")
    public TextGenerationResponse generateText(Long userId, TextGenerationRequest request) {
        // 业务逻辑 - 无需修改
        // 切面会自动进行配额检查、费用计算、配额扣除
        return adapter.generateText(request);
    }
    
    @RequiresTokenQuota(quotaType = "image", usageType = "image_generation")
    public ImageGenerationResponse generateImage(Long userId, ImageGenerationRequest request) {
        // 业务逻辑
        return adapter.generateImage(request);
    }
}
```

### 2. 注解参数说明

| 参数 | 说明 | 可选值 |
|------|------|--------|
| `quotaType` | 配额类型 | `text_token`（文本Token）<br>`image`（图片）<br>`audio`（音频）<br>`video`（视频） |
| `usageType` | 使用类型 | `text_generation`（文本生成）<br>`image_generation`（图片生成）<br>`audio_tts`（文本转语音）<br>`audio_stt`（语音转文本）<br>`video_generation`（视频生成） |

### 3. 方法签名要求

被注解的方法必须满足以下条件：

- **第一个参数必须是 `Long userId`**（用户ID）
- **第二个参数必须是请求对象**（如 `TextGenerationRequest`、`ImageGenerationRequest` 等）
- 请求对象必须包含 `provider` 和 `model` 字段

示例：
```java
@RequiresTokenQuota(quotaType = "text_token", usageType = "text_generation")
public TextGenerationResponse generateText(Long userId, TextGenerationRequest request)

@RequiresTokenQuota(quotaType = "text_token", usageType = "text_generation")
public void generateTextStream(Long userId, TextGenerationRequest request, StreamResponseHandler<TextGenerationResponse> handler)
```

## 📋 完整工作流程

```
1. 用户调用AI服务
   ↓
2. AIServiceController接收请求，从Authentication获取userId
   ↓
3. 调用AIServiceImpl方法
   ↓
4. AIBillingAspect拦截（@RequiresTokenQuota注解）
   ↓
5. 检查资源池余额（如果余额<=0，抛出异常阻止调用）
   ↓
6. 预估使用量并检查用户配额（如果不足，抛出QuotaInsufficientException）
   ↓
7. 执行AI服务调用
   ↓
8. 从响应中提取实际使用量（Token数、图片数等）
   ↓
9. 计算费用（基于模型资费配置和实际使用量）
   ↓
10. 扣除用户配额
   ↓
11. 扣除资源池余额
   ↓
12. 记录使用记录到数据库
```

## 🔧 配置步骤

### 1. 初始化数据库

确保已执行数据库迁移脚本，创建所有必要的表：
- `ai_providers` - AI提供商
- `ai_models` - AI模型
- `ai_model_pricing` - 模型资费配置
- `user_token_quota` - 用户配额
- `provider_resource_pool` - 资源池
- `ai_usage_records` - 使用记录
- 等等

### 2. 初始化模型数据

在数据库中配置AI提供商和模型：

```sql
-- 创建提供商
INSERT INTO ai_providers (name, display_name, enabled, created_at) 
VALUES ('dashscope', '阿里云通义千问', true, NOW());

-- 创建模型
INSERT INTO ai_models (provider_id, model_code, model_name, model_type, enabled, created_at)
VALUES (1, 'qwen-max', '通义千问-Max', 'text', true, NOW());

-- 创建资费配置（输入Token）
INSERT INTO ai_model_pricing (model_id, pricing_type, unit_price, unit, effective_date, is_active)
VALUES (1, 'input_token', 0.002, 'per_1k_tokens', NOW(), true);

-- 创建资费配置（输出Token）
INSERT INTO ai_model_pricing (model_id, pricing_type, unit_price, unit, effective_date, is_active)
VALUES (1, 'output_token', 0.008, 'per_1k_tokens', NOW(), true);
```

### 3. 为用户分配配额

#### 方式1：通过代码分配

```java
@Autowired
private TokenQuotaService tokenQuotaService;

// 分配文本Token配额
tokenQuotaService.grantQuota(userId, "text_token", 100000L, 
    "membership", planId, "会员开通赠送");

// 分配图片配额
tokenQuotaService.grantQuota(userId, "image", 100L, 
    "admin_grant", null, "管理员手动分配");
```

#### 方式2：通过管理后台分配

1. 登录管理后台
2. 进入 **计费管理** → **用户配额**
3. 输入用户ID，点击"查询"
4. 填写配额类型、数量、来源等信息
5. 点击"分配配额"

#### 方式3：会员订阅时自动分配

当用户开通会员时，系统可以根据订阅计划自动分配配额（需要集成会员服务）。

### 4. 充值资源池

资源池余额不足时，需要管理员手动充值：

1. 登录管理后台
2. 进入 **计费管理** → **资源池管理**
3. 找到需要充值的提供商
4. 点击"充值"按钮
5. 输入充值金额和备注
6. 确认充值

## 📊 监控和管理

### 1. 使用记录查看

**路径**：管理后台 → 计费管理 → 使用记录

- 查看所有AI服务调用的详细记录
- 支持按用户、提供商、模型、时间范围筛选
- 显示Token使用量、费用、状态等信息

### 2. 成本统计

**路径**：管理后台 → 计费管理 → 成本统计

- 查看每日成本统计汇总
- 支持按时间范围、提供商、模型筛选
- 分析成本趋势和优化策略

### 3. 资源池管理

**路径**：管理后台 → 计费管理 → 资源池管理

- 实时监控各供应商资源池状态
- 查看余额百分比和可用余额
- 在线充值资源池
- 查看余额提醒和充值历史

**自动功能**：
- 每5分钟自动检查资源池水位
- 余额低于10%时自动创建提醒
- 余额为0或负数时自动禁用该供应商下的所有模型

### 4. 用户配额管理

**路径**：管理后台 → 计费管理 → 用户配额

- 查看用户配额详情
- 手动分配配额给用户
- 查看配额使用统计

### 5. 用户实时统计（前端）

用户可以查看自己的实时使用统计：

**API端点**：
- `GET /api/billing/statistics/me` - 获取当前用户的使用统计

**前端组件**：
```tsx
import { UserUsageStatistics } from '../components/UserUsageStatistics';

<UserUsageStatistics />
```

显示内容：
- 文本Token使用情况（月度配额、总配额、已使用、可用）
- 图片生成使用情况
- 音频处理使用情况
- 视频生成使用情况
- 使用率可视化（进度条，颜色标识）

## ⚠️ 异常处理

### QuotaInsufficientException

当用户配额不足时，切面会抛出 `QuotaInsufficientException`。

**建议处理方式**：

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    @ExceptionHandler(QuotaInsufficientException.class)
    public ResponseEntity<ApiResponse<?>> handleQuotaInsufficient(QuotaInsufficientException e) {
        return ResponseEntity.status(HttpStatus.PAYMENT_REQUIRED)
                .body(ApiResponse.error(402, "配额不足: " + e.getMessage()));
    }
}
```

### PricingNotFoundException

当模型资费配置不存在时，会抛出 `PricingNotFoundException`。系统会记录警告日志，但允许调用继续执行（不计费）。

## 🔍 常见问题

### 1. 如何为新的AI服务添加计费？

只需要在方法上添加 `@RequiresTokenQuota` 注解即可，系统会自动处理计费逻辑。

```java
@RequiresTokenQuota(quotaType = "text_token", usageType = "text_generation")
public YourResponse yourMethod(Long userId, YourRequest request) {
    // 业务逻辑
}
```

### 2. 模型未配置资费会怎样？

如果模型未在计费系统中配置，调用仍然会执行，但不会计费。系统会记录警告日志：
```
未找到模型配置，跳过计费: provider=dashscope, model=qwen-max
```

### 3. 资源池余额不足会怎样？

- 系统会在调用前检查资源池余额
- 如果余额<=0，会抛出 `QuotaInsufficientException`，阻止服务调用
- 余额低于10%时，系统会自动创建提醒
- 余额为0时，会自动禁用该供应商下的所有模型

### 4. 如何查看用户的配额使用情况？

**方式1：通过管理后台**
- 进入 计费管理 → 用户配额
- 输入用户ID查询

**方式2：通过API**
```java
TokenQuotaService tokenQuotaService;
UserTokenQuota quota = tokenQuotaService.getUserQuota(userId);
```

**方式3：通过前端组件**
用户可以在前端查看自己的实时统计（如果集成了 `UserUsageStatistics` 组件）

### 5. 配额扣除是实时的吗？

是的。每次AI服务调用成功后，系统会立即：
1. 扣除用户配额
2. 扣除资源池余额
3. 记录使用记录

所有操作都在事务中执行，确保数据一致性。

### 6. 流式调用如何计费？

流式调用（如流式文本生成）会在完成后统一记录使用量。切面会包装 `StreamResponseHandler`，在 `done=true` 时进行计费。

## 📝 配额类型说明

| 配额类型 | 说明 | 单位 |
|---------|------|------|
| `text_token` | 文本Token配额 | Token数 |
| `image` | 图片生成配额 | 图片张数 |
| `audio` | 音频处理配额 | 处理次数或分钟数 |
| `video` | 视频生成配额 | 生成次数或秒数 |

## 🎯 最佳实践

1. **及时充值资源池**：建议设置余额提醒，余额低于10%时及时充值
2. **监控使用记录**：定期查看使用记录，分析用户使用模式
3. **优化模型使用**：根据成本统计，选择性价比更高的模型
4. **配额合理分配**：根据用户会员等级合理分配配额
5. **异常处理**：在前端友好地处理配额不足的异常

## 📚 相关文档

- [README.md](./README.md) - 模块结构和工作原理
- [统一接入层计费集成说明.md](./统一接入层计费集成说明.md) - 集成到AIServiceImpl的详细说明
- [用户实时统计功能说明.md](./用户实时统计功能说明.md) - 用户统计功能说明
- [资源池管理与资费提醒.md](./资源池管理与资费提醒.md) - 资源池管理详细说明
- [计费监测指南.md](./计费监测指南.md) - 如何使用管理后台监测计费

## 🆘 需要帮助？

如果遇到问题，请检查：
1. 模型是否已正确配置在数据库中
2. 用户是否有足够的配额
3. 资源池余额是否充足
4. 日志中是否有错误信息

更多技术细节请参考其他相关文档。

