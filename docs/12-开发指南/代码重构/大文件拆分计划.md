# 大文件拆分计划

**日期**: 2025-01-XX  
**目标**: 拆分三个超过1000行的大文件

## 📋 拆分目标

### 1. SettingsManagement.tsx (1,451行) → 拆分为子组件

**拆分方案**:
```
frontend/admin/components/settings/
├── SettingsManagement.tsx (主组件，< 200行)
├── GeneralSettings.tsx (通用设置)
│   ├── NotionConfig.tsx
│   ├── WechatConfig.tsx
│   ├── WechatPayConfig.tsx
│   └── AlipayConfig.tsx
├── AISettings.tsx (AI模型配置)
│   ├── ModelsManagement.tsx
│   └── RoutingStrategies.tsx
└── ApiKeyGuideModal.tsx (API Key申请引导)
```

**预计减少**: 主文件从 1,451 行 → < 200 行

---

### 2. AIService.ts (1,671行) → 提取业务逻辑服务

**拆分方案**:
```
frontend/services/ai/
├── AIService.ts (核心服务，< 300行)
├── services/
│   ├── TextGenerationService.ts
│   ├── ImageGenerationService.ts
│   ├── AudioService.ts
│   ├── VideoGenerationService.ts
│   └── BusinessLogicService.ts (业务逻辑方法)
├── adapters/ (已存在)
└── unified/ (统一接入模式)
    └── UnifiedAIService.ts
```

**预计减少**: 主文件从 1,671 行 → < 300 行

---

### 3. gemini.ts (1,525行) → 迁移到适配器模式

**拆分方案**:
```
frontend/services/ai/
├── adapters/
│   └── GeminiAdapter.ts (已存在，需要增强)
└── services/
    └── GeminiBusinessService.ts (业务逻辑方法)
```

**注意**: gemini.ts 中的业务逻辑方法（如 generateCharacterFromPrompt）应该提取到独立的服务类中，而不是放在适配器中。

**预计减少**: gemini.ts 从 1,525 行 → 迁移到适配器和服务类

---

## 🎯 拆分优先级

1. **SettingsManagement.tsx** - 最简单，先执行
2. **AIService.ts** - 中等复杂度
3. **gemini.ts** - 需要仔细处理业务逻辑迁移

---

## ✅ 拆分检查清单

- [ ] 所有文件 < 1000行
- [ ] 组件职责单一
- [ ] 向后兼容
- [ ] 无编译错误
- [ ] 功能完整

