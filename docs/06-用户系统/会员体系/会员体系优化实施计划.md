# 会员体系优化实施计划

## 项目概述
基于当前会员体系，对标主流SaaS产品，完成会员体系的前后端能力优化，结合大模型资费、盈利模式，实现完整的会员体系升级。

## 实施步骤

---

## 第一步：市场调研与需求分析（已完成）

### 1.1 已完成工作
- ✅ 分析主流SaaS会员体系（OpenAI、Midjourney、Notion等）
- ✅ 调研大模型资费结构（文本、图片、视频生成成本）
- ✅ 制定四级会员体系（免费版、基础版、专业版、企业版）
- ✅ 设计权益体系和价格策略
- ✅ 完成设计方案文档

### 1.2 输出物
- [x] `docs/会员体系优化设计方案.md` - 完整设计方案
- [x] `docs/会员体系优化实施计划.md` - 本文档

---

## 第二步：数据库模型优化

### 2.1 数据库表结构扩展

#### 2.1.1 SubscriptionPlan表扩展
**文件位置**：`backend/src/main/resources/db/migration/`（或SQL脚本）

**需要执行的SQL**：
```sql
-- 1. 配额配置字段（部分已存在，需检查）
ALTER TABLE subscription_plans 
  ADD COLUMN IF NOT EXISTS text_token_quota BIGINT DEFAULT 0 COMMENT '文本Token配额',
  ADD COLUMN IF NOT EXISTS image_generation_quota INT DEFAULT 0 COMMENT '图片生成配额',
  ADD COLUMN IF NOT EXISTS video_generation_quota INT DEFAULT 0 COMMENT '视频生成配额（秒）',
  ADD COLUMN IF NOT EXISTS api_call_quota_per_day INT DEFAULT 0 COMMENT 'API每日调用配额',
  ADD COLUMN IF NOT EXISTS storage_quota_mb INT DEFAULT 1024 COMMENT '存储配额（MB）';

-- 2. 功能权限字段（部分已存在，需检查）
ALTER TABLE subscription_plans 
  ADD COLUMN IF NOT EXISTS allow_api_access BOOLEAN DEFAULT FALSE COMMENT '允许API访问',
  ADD COLUMN IF NOT EXISTS allow_priority_queue BOOLEAN DEFAULT FALSE COMMENT '允许优先队列',
  ADD COLUMN IF NOT EXISTS allow_watermark_removal BOOLEAN DEFAULT FALSE COMMENT '允许去水印',
  ADD COLUMN IF NOT EXISTS allow_batch_processing BOOLEAN DEFAULT FALSE COMMENT '允许批量处理',
  ADD COLUMN IF NOT EXISTS allow_team_collaboration BOOLEAN DEFAULT FALSE COMMENT '允许团队协作';

-- 3. 模型权限字段
ALTER TABLE subscription_plans 
  ADD COLUMN IF NOT EXISTS allowed_ai_models TEXT COMMENT '允许使用的AI模型（JSON数组）';

-- 4. 超量付费价格
ALTER TABLE subscription_plans 
  ADD COLUMN IF NOT EXISTS overage_token_price DECIMAL(10,4) DEFAULT 0.02 COMMENT '超量Token价格（元/1K tokens）',
  ADD COLUMN IF NOT EXISTS overage_image_price DECIMAL(10,2) DEFAULT 2.00 COMMENT '超量图片价格（元/张）',
  ADD COLUMN IF NOT EXISTS overage_video_price DECIMAL(10,2) DEFAULT 0.50 COMMENT '超量视频价格（元/秒）';

-- 5. 营销字段
ALTER TABLE subscription_plans 
  ADD COLUMN IF NOT EXISTS recommended BOOLEAN DEFAULT FALSE COMMENT '是否推荐',
  ADD COLUMN IF NOT EXISTS badge_text VARCHAR(50) COMMENT '徽章文字（如：最划算、热门）';
```

#### 2.1.2 Membership表扩展
```sql
-- 配额使用记录
ALTER TABLE memberships 
  ADD COLUMN IF NOT EXISTS text_token_used BIGINT DEFAULT 0 COMMENT '已使用文本Token',
  ADD COLUMN IF NOT EXISTS image_generation_used INT DEFAULT 0 COMMENT '已使用图片生成次数',
  ADD COLUMN IF NOT EXISTS video_generation_used INT DEFAULT 0 COMMENT '已使用视频生成时长（秒）',
  ADD COLUMN IF NOT EXISTS api_calls_used_today INT DEFAULT 0 COMMENT '今日API调用次数',
  ADD COLUMN IF NOT EXISTS api_calls_reset_date DATE COMMENT 'API调用重置日期';

-- 配额重置日期
ALTER TABLE memberships 
  ADD COLUMN IF NOT EXISTS quota_reset_date DATE COMMENT '配额重置日期',
  ADD COLUMN IF NOT EXISTS last_quota_reset_date DATE COMMENT '上次配额重置日期';

-- 升级信息
ALTER TABLE memberships 
  ADD COLUMN IF NOT EXISTS upgrade_discount DECIMAL(5,2) DEFAULT 0 COMMENT '升级折扣',
  ADD COLUMN IF NOT EXISTS upgrade_from_plan_id BIGINT COMMENT '升级前计划ID';
```

#### 2.1.3 新建表：配额使用记录表
```sql
CREATE TABLE IF NOT EXISTS quota_usage_records (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    membership_id BIGINT NOT NULL COMMENT '会员ID',
    quota_type VARCHAR(50) NOT NULL COMMENT '配额类型：text_token/image/video/api_call',
    amount_used BIGINT NOT NULL COMMENT '使用量',
    quota_before BIGINT NOT NULL COMMENT '使用前配额',
    quota_after BIGINT NOT NULL COMMENT '使用后配额',
    related_record_id BIGINT COMMENT '关联记录ID',
    related_record_type VARCHAR(50) COMMENT '关联记录类型',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX idx_user_quota_type (user_id, quota_type, created_at),
    INDEX idx_membership_date (membership_id, created_at),
    INDEX idx_related_record (related_record_id, related_record_type)
) COMMENT='配额使用记录表';
```

#### 2.1.4 新建表：超量付费记录表
```sql
CREATE TABLE IF NOT EXISTS overage_charges (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用户ID',
    membership_id BIGINT NOT NULL COMMENT '会员ID',
    quota_type VARCHAR(50) NOT NULL COMMENT '配额类型',
    amount_used BIGINT NOT NULL COMMENT '超量使用量',
    unit_price DECIMAL(10,4) NOT NULL COMMENT '单价',
    total_amount DECIMAL(10,2) NOT NULL COMMENT '总金额',
    order_id BIGINT COMMENT '支付订单ID',
    status VARCHAR(20) DEFAULT 'pending' COMMENT '状态：pending/paid/failed',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    paid_at TIMESTAMP COMMENT '支付时间',
    INDEX idx_user_status (user_id, status),
    INDEX idx_order (order_id),
    INDEX idx_membership (membership_id, created_at)
) COMMENT='超量付费记录表';
```

### 2.2 实体类更新

#### 需要更新的文件：
1. `backend/src/main/java/com/heartsphere/entity/SubscriptionPlan.java`
   - 添加新字段的getter/setter
   - 添加字段注解

2. `backend/src/main/java/com/heartsphere/entity/Membership.java`
   - 添加配额使用字段
   - 添加重置日期字段

3. 新建实体类：
   - `backend/src/main/java/com/heartsphere/billing/entity/QuotaUsageRecord.java`
   - `backend/src/main/java/com/heartsphere/billing/entity/OverageCharge.java`

### 2.3 任务清单
- [ ] 编写数据库迁移SQL脚本
- [ ] 更新SubscriptionPlan实体类
- [ ] 更新Membership实体类
- [ ] 创建QuotaUsageRecord实体类
- [ ] 创建OverageCharge实体类
- [ ] 创建对应的Repository接口
- [ ] 执行数据库迁移（测试环境）
- [ ] 验证数据完整性

---

## 第三步：后端服务开发

### 3.1 配额管理服务 (QuotaManagementService)

#### 文件位置：
`backend/src/main/java/com/heartsphere/billing/service/QuotaManagementService.java`

#### 核心功能：
1. **配额查询**
   - `getQuotaInfo(Long userId)` - 获取用户配额信息
   - `getQuotaUsage(Long userId)` - 获取配额使用情况

2. **配额检查与扣减**
   - `checkQuota(Long userId, QuotaType type, Long amount)` - 检查配额
   - `consumeQuota(Long userId, QuotaType type, Long amount, String recordId)` - 扣减配额
   - `tryConsumeQuota(Long userId, QuotaType type, Long amount)` - 尝试扣减（带超量处理）

3. **配额重置**
   - `resetMonthlyQuota()` - 月度配额重置（定时任务）
   - `resetDailyQuota()` - 日度配额重置（定时任务）

4. **超量处理**
   - `handleOverage(Long userId, QuotaType type, Long amount)` - 处理超量使用

#### 任务清单：
- [ ] 创建QuotaManagementService接口和实现类
- [ ] 实现配额查询方法
- [ ] 实现配额检查和扣减方法（带事务）
- [ ] 实现配额重置方法
- [ ] 实现超量处理逻辑
- [ ] 添加单元测试
- [ ] 添加集成测试

### 3.2 权限验证服务 (MembershipPermissionService)

#### 文件位置：
`backend/src/main/java/com/heartsphere/service/MembershipPermissionService.java`

#### 核心功能：
1. **功能权限检查**
   - `canUseApi(Long userId)` - 是否可以使用API
   - `canUsePriorityQueue(Long userId)` - 是否可以使用优先队列
   - `canRemoveWatermark(Long userId)` - 是否可以去除水印
   - `canBatchProcess(Long userId)` - 是否可以批量处理
   - `canUseTeamCollaboration(Long userId)` - 是否可以使用团队协作

2. **模型权限检查**
   - `canUseModel(Long userId, String modelName)` - 是否可以使用指定模型
   - `getAllowedModels(Long userId)` - 获取允许使用的模型列表

3. **配额限制检查**
   - `getMaxSingleUseQuota(Long userId, QuotaType type)` - 获取单次使用上限

#### 任务清单：
- [ ] 创建MembershipPermissionService接口和实现类
- [ ] 实现功能权限检查方法
- [ ] 实现模型权限检查方法
- [ ] 实现配额限制检查方法
- [ ] 添加缓存机制（可选）
- [ ] 添加单元测试

### 3.3 会员升级服务 (MembershipUpgradeService)

#### 文件位置：
`backend/src/main/java/com/heartsphere/service/MembershipUpgradeService.java`

#### 核心功能：
1. **升级逻辑**
   - `upgradeMembership(Long userId, Long targetPlanId)` - 升级会员
   - `calculateUpgradePrice(Long userId, Long targetPlanId)` - 计算升级价格
   - `calculateProRatedQuota(Membership current, SubscriptionPlan target)` - 按比例计算配额

2. **降级逻辑**
   - `downgradeMembership(Long userId, Long targetPlanId)` - 降级会员
   - `handleDowngradeQuota(Membership current, SubscriptionPlan target)` - 处理降级配额

#### 任务清单：
- [ ] 创建MembershipUpgradeService接口和实现类
- [ ] 实现升级逻辑（价格计算、配额转换）
- [ ] 实现降级逻辑
- [ ] 添加升级/降级验证
- [ ] 添加单元测试

### 3.4 使用统计服务 (UsageStatisticsService)

#### 文件位置：
`backend/src/main/java/com/heartsphere/billing/service/UsageStatisticsService.java`

#### 核心功能：
1. **实时统计**
   - `getCurrentPeriodStats(Long userId)` - 获取当前周期统计
   - `getQuotaUsageRate(Long userId)` - 获取配额使用率

2. **历史统计**
   - `getDailyUsage(Long userId, LocalDate start, LocalDate end)` - 获取每日使用统计
   - `getMonthlyUsage(Long userId, int months)` - 获取月度使用统计

3. **成本分析**
   - `calculateUsageCost(Long userId, LocalDate start, LocalDate end)` - 计算使用成本
   - `getCostBreakdown(Long userId)` - 获取成本分解

#### 任务清单：
- [ ] 创建UsageStatisticsService接口和实现类
- [ ] 实现实时统计方法
- [ ] 实现历史统计方法
- [ ] 实现成本分析方法
- [ ] 添加数据聚合优化
- [ ] 添加单元测试

### 3.5 API接口开发

#### 3.5.1 会员配额API (MembershipQuotaController)

**文件位置**：`backend/src/main/java/com/heartsphere/controller/MembershipQuotaController.java`

**接口列表**：
- `GET /api/membership/quota` - 获取当前配额信息
- `GET /api/membership/quota/usage` - 获取配额使用情况
- `GET /api/membership/quota/usage/history` - 获取使用历史
- `GET /api/membership/permissions` - 获取权限信息

#### 3.5.2 升级/降级API

**扩展MembershipController**：
- `POST /api/membership/upgrade` - 升级会员
- `POST /api/membership/downgrade` - 降级会员
- `GET /api/membership/upgrade/price` - 获取升级价格

#### 任务清单：
- [ ] 创建MembershipQuotaController
- [ ] 扩展MembershipController（升级/降级接口）
- [ ] 实现所有API接口
- [ ] 添加接口文档（Swagger）
- [ ] 添加接口测试

### 3.6 定时任务

#### 文件位置：
`backend/src/main/java/com/heartsphere/scheduler/QuotaResetScheduler.java`

#### 任务：
1. **月度配额重置**：每月1日0点执行
2. **日度配额重置**：每日0点执行（API调用配额）

#### 任务清单：
- [ ] 创建QuotaResetScheduler类
- [ ] 实现月度重置任务
- [ ] 实现日度重置任务
- [ ] 添加任务日志
- [ ] 添加异常处理
- [ ] 测试定时任务

---

## 第四步：前端功能开发

### 4.1 配额仪表盘组件

#### 文件位置：
`frontend/src/components/membership/QuotaDashboard.tsx`

#### 功能：
- 显示各类配额使用情况（进度条）
- 配额使用百分比
- 剩余配额显示
- 重置时间显示

#### 任务清单：
- [ ] 创建QuotaDashboard组件
- [ ] 实现配额卡片UI
- [ ] 集成配额API
- [ ] 添加加载状态
- [ ] 添加错误处理
- [ ] 添加样式（CSS）

### 4.2 使用统计页面

#### 文件位置：
`frontend/src/pages/MembershipUsage.tsx`

#### 功能：
- 使用趋势图表（使用Chart.js或ECharts）
- 使用明细列表
- 日期范围筛选
- 导出功能

#### 任务清单：
- [ ] 创建MembershipUsage页面
- [ ] 集成图表库
- [ ] 实现使用趋势图
- [ ] 实现使用明细列表
- [ ] 实现筛选功能
- [ ] 添加样式

### 4.3 会员中心优化

#### 文件位置：
`frontend/src/pages/MembershipCenter.tsx`（或现有页面）

#### 优化内容：
1. **配额展示区域**：集成QuotaDashboard组件
2. **升级推荐**：基于使用情况推荐升级
3. **使用统计入口**：链接到使用统计页面
4. **快速升级按钮**：一键升级功能

#### 任务清单：
- [ ] 优化会员中心页面布局
- [ ] 集成配额仪表盘
- [ ] 实现升级推荐逻辑
- [ ] 添加快捷操作入口
- [ ] 优化响应式布局

### 4.4 升级引导流程

#### 文件位置：
`frontend/src/components/membership/UpgradeModal.tsx`

#### 功能：
- 升级前后对比
- 价格计算（按比例）
- 升级确认
- 支付流程

#### 任务清单：
- [ ] 创建UpgradeModal组件
- [ ] 实现对比展示
- [ ] 实现价格计算
- [ ] 集成支付流程
- [ ] 添加样式

### 4.5 配额使用提示

#### 功能：
- 配额不足警告（80%、90%、100%阈值）
- 超量使用提示
- 升级建议弹窗

#### 任务清单：
- [ ] 创建QuotaAlert组件
- [ ] 实现配额监控逻辑
- [ ] 实现警告提示
- [ ] 实现升级建议弹窗
- [ ] 集成到全局状态管理

### 4.6 API类型定义

#### 文件位置：
`frontend/src/services/api/membership/types.ts`

#### 需要添加的类型：
```typescript
export interface QuotaInfo {
  textTokenQuota: number;
  textTokenUsed: number;
  imageQuota: number;
  imageUsed: number;
  videoQuota: number;
  videoUsed: number;
  quotaResetDate: string;
}

export interface UsageStats {
  currentPeriod: QuotaInfo;
  dailyUsage: DailyUsage[];
  monthlyUsage: MonthlyUsage[];
}

export interface UpgradePrice {
  targetPlanId: number;
  targetPlanName: string;
  price: number;
  proRatedAmount: number;
  totalPrice: number;
}
```

#### 任务清单：
- [ ] 更新types.ts文件
- [ ] 添加新类型定义
- [ ] 更新API接口定义

---

## 第五步：测试与优化

### 5.1 单元测试

#### 后端测试：
- [ ] QuotaManagementService单元测试
- [ ] MembershipPermissionService单元测试
- [ ] MembershipUpgradeService单元测试
- [ ] UsageStatisticsService单元测试
- [ ] Controller单元测试

#### 前端测试：
- [ ] QuotaDashboard组件测试
- [ ] MembershipUsage页面测试
- [ ] UpgradeModal组件测试
- [ ] API调用测试

### 5.2 集成测试

#### 测试场景：
- [ ] 配额查询和扣减流程
- [ ] 配额重置流程
- [ ] 升级/降级流程
- [ ] 超量付费流程
- [ ] 权限验证流程

### 5.3 性能测试

#### 测试项：
- [ ] 配额查询性能（目标：<100ms）
- [ ] 配额扣减性能（目标：<200ms）
- [ ] 并发扣减测试
- [ ] 大数据量统计查询性能

### 5.4 用户体验测试

#### 测试项：
- [ ] 配额展示清晰度
- [ ] 升级流程顺畅度
- [ ] 使用统计可读性
- [ ] 提示信息有效性

### 5.5 数据准确性验证

#### 验证项：
- [ ] 配额计算准确性
- [ ] 使用量统计准确性
- [ ] 价格计算准确性
- [ ] 重置逻辑正确性

---

## 第六步：上线与监控

### 6.1 灰度发布

#### 发布策略：
1. **内测阶段**：内部员工测试（1周）
2. **小范围灰度**：10%用户（1周）
3. **中范围灰度**：50%用户（1周）
4. **全量发布**：100%用户

### 6.2 监控指标

#### 关键指标：
- 配额查询QPS
- 配额扣减成功率
- 配额重置任务执行情况
- 超量付费订单数
- 升级/降级操作数
- API响应时间
- 错误率

### 6.3 告警设置

#### 告警项：
- 配额重置任务失败
- 配额扣减失败率>1%
- API响应时间>500ms
- 数据库连接异常
- 超量付费订单支付失败率>5%

### 6.4 用户反馈收集

#### 收集渠道：
- 用户反馈表单
- 客服反馈
- 用户调研
- 数据分析

### 6.5 持续优化

#### 优化方向：
- 根据用户反馈优化功能
- 根据数据优化价格策略
- 优化配额分配策略
- 优化用户体验

---

## 项目时间表

| 步骤 | 预估时间 | 负责人 | 状态 |
|------|----------|--------|------|
| 第一步：市场调研与需求分析 | 3天 | - | ✅ 已完成 |
| 第二步：数据库模型优化 | 5天 | - | ⏳ 待开始 |
| 第三步：后端服务开发 | 15天 | - | ⏳ 待开始 |
| 第四步：前端功能开发 | 10天 | - | ⏳ 待开始 |
| 第五步：测试与优化 | 7天 | - | ⏳ 待开始 |
| 第六步：上线与监控 | 5天 | - | ⏳ 待开始 |
| **总计** | **45天** | - | - |

---

## 风险与应对

### 技术风险
1. **数据库迁移风险**：先在测试环境充分测试
2. **并发问题**：使用分布式锁，充分测试并发场景
3. **性能问题**：添加缓存，优化查询

### 业务风险
1. **用户流失**：合理的定价，良好的用户体验
2. **成本失控**：设置成本上限，监控异常使用
3. **数据错误**：充分的测试，数据校验

---

## 总结

本实施计划详细分解了会员体系优化的六个步骤，每个步骤都有明确的任务清单和交付物。按照此计划执行，可以系统性地完成会员体系的全面优化。

**下一步行动**：开始执行第二步 - 数据库模型优化
