# 心域连接功能需求分析与设计文档 - 第三阶段

**文档版本**: V1.0  
**编写日期**: 2025-12-22  
**功能模块**: 访问他人心域体验系统  
**目标**: 实现访问他人心域后的体验功能，支持体验模式、数据隔离和暖心互动

---

## 一、需求概述

### 1.1 背景

当用户通过连接请求进入他人的心域后，需要在其中进行体验。这种体验应该是：
1. **沉浸式的**：能够真实感受心域的氛围和内容
2. **数据隔离的**：访问者的体验数据不保存，不影响主人的心域
3. **有温度的**：能够与主人进行暖心互动，留下美好的记忆

### 1.2 核心需求

- ✅ **体验模式**：访问者在他人心域中体验，数据不保存
- ✅ **完整体验**：可以体验场景、E-SOUL对话等核心功能
- ✅ **数据隔离**：访问者的体验数据与主人的数据完全隔离
- ✅ **暖心留言**：访问者可以给主人留下暖心的留言
- ✅ **体验记录**：主人可以看到访问者的体验摘要（可选）
- ✅ **离开机制**：访问者可以随时离开，离开时可以选择留下留言

### 1.3 设计目标

1. **体验完整性**：访问者能够完整体验心域的核心功能
2. **数据安全**：确保访问者的数据不影响主人的心域
3. **情感连接**：通过暖心留言等功能增强情感连接
4. **体验流畅**：体验过程流畅自然，没有割裂感

---

## 二、体验模式设计

### 2.1 体验模式标识

#### 2.1.1 视觉标识详细设计

**功能描述**: 在体验模式下，界面应该有清晰的视觉标识，让用户明确知道当前处于体验模式

**详细需求**:

1. **顶部标识栏设计**

   **完整标识栏布局**:
   ```
   ┌─────────────────────────────────────────────────┐
   │ 🌟 正在体验 [心域名称] 的心域                   │
   │    ⚠️ 体验模式下，内容不会保存到主人的心域    │
   │    [收起]  [离开]                              │
   └─────────────────────────────────────────────────┘
   ```

   **组件数据结构**:
   ```typescript
   interface ExperienceModeBannerProps {
     heartSphereName: string; // 心域名称
     ownerName?: string; // 主人名称
     onCollapse?: () => void; // 收起回调
     onLeave?: () => void; // 离开回调
   }
   
   interface ExperienceModeState {
     isCollapsed: boolean; // 是否已收起
     isVisible: boolean; // 是否可见
   }
   ```

   **React组件实现**:
   ```typescript
   // ExperienceModeBanner.tsx
   const ExperienceModeBanner: React.FC<ExperienceModeBannerProps> = ({
     heartSphereName,
     ownerName,
     onCollapse,
     onLeave
   }) => {
     const [isCollapsed, setIsCollapsed] = useState(false);
     
     const handleCollapse = () => {
       setIsCollapsed(true);
       onCollapse?.();
     };
     
     const handleLeave = () => {
       if (confirm('确定要离开体验吗？你可以选择留下暖心留言。')) {
         onLeave?.();
       }
     };
     
     if (isCollapsed) {
       // 收起状态：显示最小化标识
       return (
         <div className="experience-banner-collapsed">
           <button
             className="expand-button"
             onClick={() => setIsCollapsed(false)}
             title="展开体验模式标识"
           >
             🌟 体验模式
           </button>
         </div>
       );
     }
     
     return (
       <div className="experience-mode-banner">
         <div className="banner-content">
           <div className="banner-icon">🌟</div>
           <div className="banner-text">
             <div className="banner-title">
               正在体验 <span className="heartsphere-name">{heartSphereName}</span> 的心域
               {ownerName && <span className="owner-name">（主人：{ownerName}）</span>}
             </div>
             <div className="banner-hint">
               ⚠️ 体验模式下，内容不会保存到主人的心域
             </div>
           </div>
           <div className="banner-actions">
             <button className="collapse-button" onClick={handleCollapse}>
               收起
             </button>
             <button className="leave-button" onClick={handleLeave}>
               离开
             </button>
           </div>
         </div>
       </div>
     );
   };
   ```

   **CSS样式设计**:
   ```css
   .experience-mode-banner {
     position: sticky;
     top: 0;
     z-index: 100;
     background: linear-gradient(135deg, 
       rgba(255, 245, 238, 0.95) 0%,
       rgba(232, 244, 248, 0.95) 100%);
     border-bottom: 2px solid rgba(127, 184, 209, 0.3);
     box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
     backdrop-filter: blur(10px);
   }
   
   .banner-content {
     display: flex;
     align-items: center;
     padding: 12px 20px;
     gap: 16px;
     max-width: 1400px;
     margin: 0 auto;
   }
   
   .banner-icon {
     font-size: 24px;
     animation: star-twinkle 2s ease-in-out infinite;
   }
   
   @keyframes star-twinkle {
     0%, 100% { opacity: 1; transform: scale(1); }
     50% { opacity: 0.7; transform: scale(1.1); }
   }
   
   .banner-text {
     flex: 1;
     display: flex;
     flex-direction: column;
     gap: 4px;
   }
   
   .banner-title {
     font-size: 16px;
     font-weight: 600;
     color: #333;
   }
   
   .heartsphere-name {
     color: #FF9999;
     font-weight: 700;
   }
   
   .owner-name {
     color: #666;
     font-size: 14px;
     font-weight: normal;
   }
   
   .banner-hint {
     font-size: 13px;
     color: #999;
   }
   
   .banner-actions {
     display: flex;
     gap: 12px;
   }
   
   .collapse-button,
   .leave-button {
     padding: 6px 16px;
     border: none;
     border-radius: 6px;
     font-size: 14px;
     cursor: pointer;
     transition: all 0.2s;
   }
   
   .collapse-button {
     background: rgba(127, 184, 209, 0.2);
     color: #4A90A4;
   }
   
   .collapse-button:hover {
     background: rgba(127, 184, 209, 0.3);
   }
   
   .leave-button {
     background: #FF9999;
     color: white;
   }
   
   .leave-button:hover {
     background: #FF8888;
   }
   
   /* 收起状态 */
   .experience-banner-collapsed {
     position: sticky;
     top: 0;
     z-index: 100;
     padding: 4px 20px;
     background: rgba(255, 245, 238, 0.9);
     border-bottom: 1px solid rgba(127, 184, 209, 0.2);
   }
   
   .expand-button {
     background: none;
     border: none;
     color: #4A90A4;
     font-size: 13px;
     cursor: pointer;
     padding: 4px 8px;
     border-radius: 4px;
     transition: background 0.2s;
   }
   
   .expand-button:hover {
     background: rgba(127, 184, 209, 0.1);
   }
   ```

2. **标识样式详细设计**

   **标识栏状态**:
   - **展开状态**：显示完整信息（心域名称、提示文字、操作按钮）
   - **收起状态**：显示最小化标识（仅显示"🌟 体验模式"）
   - **可收起**：用户可以选择收起以节省空间
   - **自动收起**：可选功能，体验一段时间后自动收起

3. **颜色主题详细规范**

   **背景色方案**:
   ```css
   /* 方案一：淡蓝色系（推荐） */
   .experience-mode-banner {
     background: linear-gradient(135deg, 
       rgba(232, 244, 248, 0.95) 0%,
       rgba(207, 231, 243, 0.95) 100%);
     border-bottom-color: rgba(127, 184, 209, 0.3);
   }
   
   /* 方案二：淡紫色系 */
   .experience-mode-banner.purple-theme {
     background: linear-gradient(135deg, 
       rgba(243, 229, 245, 0.95) 0%,
       rgba(225, 190, 231, 0.95) 100%);
     border-bottom-color: rgba(186, 104, 200, 0.3);
   }
   
   /* 方案三：淡黄色系 */
   .experience-mode-banner.yellow-theme {
     background: linear-gradient(135deg, 
       rgba(255, 248, 225, 0.95) 0%,
       rgba(255, 236, 179, 0.95) 100%);
     border-bottom-color: rgba(255, 213, 79, 0.3);
   }
   ```

   **文字颜色规范**:
   - **主标题**：`#333`（深灰色）
   - **心域名称**：`#FF9999`（暖粉色，突出显示）
   - **主人名称**：`#666`（中灰色）
   - **提示文字**：`#999`（浅灰色）

   **图标设计**:
   - **主图标**：🌟（星号，表达体验的特殊性）
   - **警告图标**：⚠️（提示数据不会保存）
   - **动画效果**：星星闪烁动画（`star-twinkle`）

#### 2.1.2 功能限制提示

**功能描述**: 在体验模式下，某些功能需要提示限制

**详细需求**:

1. **数据保存提示**
   - **对话提示**：与E-SOUL对话时提示"体验模式，对话不会保存"
   - **操作提示**：某些操作（如收藏、创建内容）提示不可用
   - **友好提示**：提示文字友好，不是冷冰冰的禁止

2. **功能限制说明**
   - **可体验功能**：明确说明可以体验哪些功能
   - **不可用功能**：说明哪些功能在体验模式下不可用
   - **原因说明**：简单说明限制的原因（保护主人的心域）

### 2.2 体验功能范围

#### 2.2.1 可体验功能

**功能描述**: 访问者可以体验的核心功能

**详细需求**:

1. **场景浏览**
   - **场景列表**：可以浏览所有共享的场景
   - **场景切换**：可以在场景间自由切换
   - **场景详情**：可以查看场景的详细信息

2. **E-SOUL交互**
   - **角色列表**：可以查看场景中的E-SOUL列表
   - **角色详情**：可以查看E-SOUL的详细信息
   - **对话功能**：可以与E-SOUL进行对话交互
   - **对话历史**：在当前会话中可以查看对话历史（但不会保存）

3. **内容浏览**
   - **剧本体验**：可以体验场景中的剧本（如果共享）
   - **记忆浏览**：可以浏览场景的记忆（如果共享）
   - **视觉体验**：可以体验场景的视觉设计

4. **基础交互**
   - **界面导航**：可以正常使用界面导航功能
   - **设置查看**：可以查看场景设置（但不可修改）
   - **搜索功能**：可以使用搜索功能查找内容

#### 2.2.2 不可用功能

**功能描述**: 体验模式下不可用的功能

**详细需求**:

1. **数据创建**
   - **创建角色**：不能创建新的E-SOUL
   - **创建场景**：不能创建新场景
   - **创建剧本**：不能创建新剧本
   - **创建日记**：不能创建日记（但可以体验日记功能）

2. **数据修改**
   - **修改内容**：不能修改任何现有内容
   - **删除内容**：不能删除任何内容
   - **设置修改**：不能修改场景或心域的设置

3. **数据保存**
   - **对话保存**：对话内容不会保存
   - **状态保存**：体验状态不会保存
   - **偏好保存**：用户偏好不会保存

4. **权限相关**
   - **管理功能**：不能使用任何管理功能
   - **分享功能**：不能分享或转发内容
   - **导出功能**：不能导出数据

#### 2.2.3 功能降级处理

**功能描述**: 对于不可用的功能，提供友好的降级方案

**详细需求**:

1. **按钮禁用**
   - **视觉禁用**：不可用的按钮显示为禁用状态
   - **提示说明**：鼠标悬停时显示禁用原因
   - **友好提示**：提示文字友好，说明这是体验模式

2. **功能替代**
   - **查看替代**：不能编辑时，提供查看模式
   - **预览替代**：不能创建时，提供预览功能
   - **说明替代**：不能操作时，提供说明文字

3. **引导提示**
   - **引导创建**：提示用户可以在自己的心域中创建
   - **引导分享**：引导用户分享自己的心域
   - **引导连接**：引导用户与心域主人建立连接

### 2.3 数据隔离机制

#### 2.3.1 数据存储隔离

**功能描述**: 确保访问者的体验数据与主人的数据完全隔离

**详细需求**:

1. **会话隔离**
   - **临时会话**：访问者的对话使用临时会话存储
   - **会话标识**：使用唯一的会话ID标识访问者
   - **会话清理**：离开时清理临时会话数据

2. **状态隔离**
   - **独立状态**：访问者的体验状态完全独立
   - **状态标识**：使用访问者ID和会话ID标识状态
   - **状态清理**：离开时清理所有临时状态

3. **存储策略**
   - **本地存储**：体验数据存储在访问者的浏览器本地
   - **临时存储**：使用SessionStorage存储临时数据
   - **自动清理**：关闭浏览器或离开时自动清理

#### 2.3.2 数据同步隔离

**功能描述**: 确保体验数据不会同步到服务器或影响主人数据

**详细需求**:

1. **API隔离**
   - **体验模式标识**：所有API请求携带体验模式标识
   - **数据过滤**：服务器端过滤体验模式的数据
   - **权限检查**：服务器端验证体验权限

2. **数据流向**
   - **只读模式**：体验模式下只读取主人的共享数据
   - **不写数据**：不向服务器写入任何数据（除留言外）
   - **本地处理**：所有体验数据在本地处理

3. **安全机制**
   - **权限验证**：验证访问者的连接权限
   - **时间限制**：可以设置体验时长限制
   - **访问日志**：记录访问日志（但不记录详细内容）

### 2.4 体验流程

#### 2.4.1 进入体验

**功能描述**: 访问者进入他人心域后的初始流程

**详细需求**:

1. **欢迎界面**
   - **欢迎动画**：进入时显示欢迎动画
   - **欢迎文字**：显示友好的欢迎文字
   - **体验说明**：简要说明体验模式和注意事项

2. **权限验证**
   - **连接验证**：验证连接状态是否有效
   - **权限检查**：检查访问权限
   - **错误处理**：如果权限无效，友好提示并引导

3. **场景加载**
   - **场景列表**：加载共享的场景列表
   - **默认场景**：自动进入第一个场景或默认场景
   - **加载提示**：加载时显示友好的加载提示

#### 2.4.2 体验过程

**功能描述**: 访问者在心域中的体验过程

**详细需求**:

1. **场景切换**
   - **自由切换**：可以在共享的场景间自由切换
   - **切换动画**：场景切换时有平滑的过渡动画
   - **状态保持**：在当前会话中保持场景状态（但不保存）

2. **E-SOUL对话**
   - **对话入口**：可以点击E-SOUL进入对话
   - **对话体验**：完整的对话体验（流式响应、多轮对话等）
   - **对话提示**：对话界面显示体验模式提示

3. **内容浏览**
   - **内容访问**：可以浏览共享的内容
   - **浏览限制**：某些敏感内容可能不可访问
   - **浏览记录**：在本地记录浏览历史（但不保存到服务器）

#### 2.4.3 离开体验

**功能描述**: 访问者离开心域时的流程

**详细需求**:

1. **离开确认**
   - **确认对话框**：离开时显示确认对话框
   - **留言提示**：提示可以留下暖心留言
   - **取消选项**：可以取消离开，继续体验

2. **数据清理**
   - **会话清理**：清理临时会话数据
   - **状态清理**：清理所有临时状态
   - **缓存清理**：清理体验相关的缓存

3. **离开动画**
   - **过渡动画**：离开时有友好的过渡动画
   - **感谢文字**：显示感谢访问的文字
   - **引导返回**：引导返回心域连接界面

---

## 三、暖心留言系统

### 3.1 留言功能设计

#### 3.1.1 留言类型和建议

**功能描述**: 支持多种类型的暖心留言，让访问者能够以温暖的方式与主人互动

**设计建议**:

我们建议采用**多层次的留言方式**，从简单到丰富，让用户有更多选择：

1. **文本留言（核心功能）**
   - **文字内容**：用户可以输入文字留言
   - **字数限制**：建议100-300字（保持简短温馨）
   - **格式支持**：支持基本的文本格式（如换行）
   - **快速模板**：提供快速留言模板（如"谢谢你分享这么美好的心域！"）

2. **表情留言（增强表达）**
   - **表情选择**：可以选择表情符号
   - **表情组合**：可以组合多个表情（如"✨💙⭐"）
   - **表情推荐**：根据场景推荐合适的表情

3. **卡片留言（推荐功能）**
   - **电子卡片**：使用精美的电子卡片留言
   - **卡片模板**：
     - **感谢卡片**："谢谢你分享这个心域，让我感受到了温暖！"
     - **赞美卡片**："你的心域设计得太棒了，每个细节都很用心！"
     - **祝福卡片**："希望你的心域越来越美好！"
     - **共鸣卡片**："在这里找到了共鸣，谢谢你创造这个空间！"
   - **卡片定制**：可以自定义卡片文字和样式

4. **图片留言（可选）**
   - **图片上传**：可以上传图片作为留言（如手绘、截图等）
   - **图片大小**：限制图片大小（如2MB）
   - **图片格式**：支持常见图片格式（JPG、PNG）
   - **使用场景**：分享体验截图、手绘感谢图等

5. **语音留言（可选，增强温度）**
   - **语音录制**：可以录制语音留言（60秒以内）
   - **语音播放**：主人可以播放语音留言
   - **语音转文字**：自动生成文字摘要（可选）
   - **使用场景**：当文字无法表达时，用语音传达更真实的情感

#### 3.1.2 暖心留言内容建议

**功能描述**: 提供留言内容建议，帮助用户写出温暖的留言

**详细需求**:

1. **留言主题分类**

   **感谢类留言**
   - "谢谢你分享这么美好的心域，让我感受到了温暖！✨"
   - "在这里的体验让我很感动，谢谢你！💙"
   - "你的E-SOUL真的很特别，谢谢你的分享！⭐"

   **赞美类留言**
   - "你的心域设计得太棒了，每个细节都很用心！"
   - "这些E-SOUL都很有个性，很喜欢！"
   - "你的心域给了我很多启发，谢谢！"

   **情感类留言**
   - "在这里找到了共鸣，谢谢你创造这个空间！"
   - "体验你的心域让我感到温暖和治愈！"
   - "希望我也能创造出这么美好的心域！"

   **鼓励类留言**
   - "继续加油，你的心域一定会越来越美好！"
   - "看到你这么用心，真的很佩服！"
   - "期待看到更多精彩的内容！"

   **分享类留言**
   - "我在[某个场景]遇到了[某个E-SOUL]，对话很愉快！"
   - "最喜欢[某个场景]的氛围，很有感觉！"
   - "感谢你让我体验了这么特别的心域！"

2. **留言模板库**
   - **快速模板**：提供10-15个快速留言模板
   - **模板分类**：按主题分类（感谢、赞美、情感、鼓励）
   - **模板定制**：用户可以基于模板修改
   - **个性化模板**：系统可以基于体验内容生成个性化建议

3. **AI辅助建议**
   - **智能建议**：基于体验内容AI生成留言建议
   - **情感匹配**：根据体验感受推荐合适的留言风格
   - **内容提示**：提示用户可以提及的具体内容（如场景、E-SOUL）

#### 3.1.3 留言场景和时机

**功能描述**: 用户可以在哪些场景留下留言，以及最佳的留言时机

**详细需求**:

1. **离开时留言（主要场景）**
   - **离开对话框**：在离开确认对话框中提供留言入口
   - **提醒提示**：友好提示"想给主人留下一些话吗？"
   - **快捷留言**：提供3-5个快捷留言模板，一键选择
   - **完整留言**：可以点击"写更多"进入完整的留言界面
   - **可选性**：留言是可选的，可以选择"直接离开"或"留下留言"

2. **体验中留言（增强互动）**
   - **留言按钮**：在体验界面右上角提供留言按钮（小图标）
   - **浮动按钮**：可以使用浮动的留言按钮（不遮挡内容）
   - **随时留言**：可以随时打开留言界面，不中断体验
   - **最小化设计**：留言按钮设计要小，不打扰体验

3. **特定场景留言（情感触发）**
   - **对话后留言**：与E-SOUL有深度对话后，提示可以留言
   - **场景浏览后留言**：浏览完所有共享场景后，提示留言
   - **触发式留言**：在特定时刻提示留言（如体验30分钟后，有良好体验时）
   - **情感触发**：当体验产生强烈情感共鸣时，提示留言

4. **留言时机建议**

   **最佳留言时机**：
   - 体验结束后，离开前
   - 有特别喜欢的场景或E-SOUL时
   - 体验产生共鸣或感动时
   - 想要表达感谢时

   **不建议的时机**：
   - 体验刚开始（还未充分体验）
   - 体验过程中频繁提示（会打扰体验）

#### 3.1.4 留言界面详细设计

**功能描述**: 留言界面的设计和交互，提供温馨、易用的体验

**详细需求**:

1. **留言界面布局详细设计**

   **界面一：快速留言（离开时）**

   **数据结构**:
   ```typescript
   interface QuickMessageModalProps {
     heartSphereName: string;
     ownerName?: string;
     quickTemplates: string[]; // 快速留言模板
     onSend: (message: string) => void;
     onCancel: () => void;
     onWriteMore: () => void;
   }
   ```

   **React组件实现**:
   ```typescript
   // QuickMessageModal.tsx
   const QuickMessageModal: React.FC<QuickMessageModalProps> = ({
     heartSphereName,
     ownerName,
     quickTemplates,
     onSend,
     onCancel,
     onWriteMore
   }) => {
     const [selectedTemplate, setSelectedTemplate] = useState<string | null>(null);
     
     const handleSend = () => {
       if (selectedTemplate) {
         onSend(selectedTemplate);
       }
     };
     
     return (
       <Modal isOpen={true} onClose={onCancel} className="quick-message-modal">
         <div className="modal-header">
           <h3>体验即将结束</h3>
           <button className="close-button" onClick={onCancel}>×</button>
         </div>
         
         <div className="modal-content">
           <div className="greeting-text">
             想给 <span className="heartsphere-name">{heartSphereName}</span> 的主人留下一些话吗？
             {ownerName && <span className="owner-name">（{ownerName}）</span>}
           </div>
           
           <div className="quick-templates">
             <div className="template-label">快速留言：</div>
             <div className="template-list">
               {quickTemplates.map((template, index) => (
                 <div
                   key={index}
                   className={`template-item ${
                     selectedTemplate === template ? 'selected' : ''
                   }`}
                   onClick={() => setSelectedTemplate(template)}
                 >
                   {template}
                 </div>
               ))}
             </div>
           </div>
           
           <button className="write-more-button" onClick={onWriteMore}>
             写更多...
           </button>
         </div>
         
         <div className="modal-footer">
           <button className="cancel-button" onClick={onCancel}>
             直接离开
           </button>
           <button
             className="send-button"
             onClick={handleSend}
             disabled={!selectedTemplate}
           >
             发送留言
           </button>
         </div>
       </Modal>
     );
   };
   ```

   **界面二：完整留言（写更多）**

   **数据结构**:
   ```typescript
   interface FullMessageModalProps {
     heartSphereName: string;
     ownerName?: string;
     initialContent?: string; // 初始内容（如果从快速模板进入）
     onSend: (message: WarmMessage) => void;
     onCancel: () => void;
   }
   
   interface WarmMessage {
     content: string;
     contentType: 'text' | 'card' | 'image' | 'audio';
     imageUrl?: string;
     audioUrl?: string;
     audioDuration?: number;
     cardData?: CardData;
   }
   ```

   **React组件实现**:
   ```typescript
   // FullMessageModal.tsx
   const FullMessageModal: React.FC<FullMessageModalProps> = ({
     heartSphereName,
     ownerName,
     initialContent = '',
     onSend,
     onCancel
   }) => {
     const [content, setContent] = useState(initialContent);
     const [contentType, setContentType] = useState<'text' | 'card' | 'image' | 'audio'>('text');
     const [imageUrl, setImageUrl] = useState<string | undefined>();
     const [audioUrl, setAudioUrl] = useState<string | undefined>();
     const [cardData, setCardData] = useState<CardData | undefined>();
     const [showCardMaker, setShowCardMaker] = useState(false);
     const [showImageUpload, setShowImageUpload] = useState(false);
     const [showAudioRecorder, setShowAudioRecorder] = useState(false);
     
     const maxLength = 300;
     const remainingChars = maxLength - content.length;
     
     const handleSend = () => {
       if (content.trim() || contentType !== 'text') {
         const message: WarmMessage = {
           content: content.trim(),
           contentType,
           imageUrl,
           audioUrl,
           cardData
         };
         onSend(message);
       }
     };
     
     return (
       <Modal isOpen={true} onClose={onCancel} className="full-message-modal">
         <div className="modal-header">
           <h3>
             给 <span className="heartsphere-name">{heartSphereName}</span> 的主人留言
             {ownerName && <span className="owner-name">（{ownerName}）</span>}
           </h3>
           <button className="close-button" onClick={onCancel}>×</button>
         </div>
         
         <div className="modal-content">
           <div className="prompt-text">你想对主人说什么？</div>
           
           {/* 留言输入区 */}
           <div className="message-input-area">
             <textarea
               className="message-textarea"
               value={content}
               onChange={(e) => {
                 if (e.target.value.length <= maxLength) {
                   setContent(e.target.value);
                 }
               }}
               placeholder="输入你的留言..."
               rows={6}
               disabled={contentType === 'card'}
             />
             
             {/* 字数统计 */}
             <div className={`char-count ${remainingChars < 20 ? 'warning' : ''}`}>
               已输入 {content.length}/{maxLength} 字
             </div>
           </div>
           
           {/* 内容类型选择 */}
           <div className="content-type-selector">
             <button
               className={`type-button ${contentType === 'text' ? 'active' : ''}`}
               onClick={() => setContentType('text')}
             >
               <span>😊</span>
               <span>文字</span>
             </button>
             <button
               className={`type-button ${contentType === 'card' ? 'active' : ''}`}
               onClick={() => setShowCardMaker(true)}
             >
               <span>💌</span>
               <span>卡片</span>
             </button>
             <button
               className={`type-button ${contentType === 'image' ? 'active' : ''}`}
               onClick={() => setShowImageUpload(true)}
             >
               <span>📷</span>
               <span>图片</span>
             </button>
             <button
               className={`type-button ${contentType === 'audio' ? 'active' : ''}`}
               onClick={() => setShowAudioRecorder(true)}
             >
               <span>🎤</span>
               <span>语音</span>
             </button>
           </div>
           
           {/* 卡片预览 */}
           {contentType === 'card' && cardData && (
             <div className="card-preview">
               <CardPreview card={cardData} />
             </div>
           )}
           
           {/* 图片预览 */}
           {contentType === 'image' && imageUrl && (
             <div className="image-preview">
               <img src={imageUrl} alt="留言图片" />
               <button
                 className="remove-image-button"
                 onClick={() => {
                   setImageUrl(undefined);
                   setContentType('text');
                 }}
               >
                 ×
               </button>
             </div>
           )}
           
           {/* 语音预览 */}
           {contentType === 'audio' && audioUrl && (
             <div className="audio-preview">
               <AudioPlayer src={audioUrl} />
               <button
                 className="remove-audio-button"
                 onClick={() => {
                   setAudioUrl(undefined);
                   setContentType('text');
                 }}
               >
                 ×
               </button>
             </div>
           )}
           
           {/* AI留言建议 */}
           <div className="ai-suggestion">
             <div className="suggestion-icon">💡</div>
             <div className="suggestion-text">
               "可以提及你最喜欢的场景或E-SOUL，让留言更个性化"
             </div>
           </div>
         </div>
         
         <div className="modal-footer">
           <button className="cancel-button" onClick={onCancel}>
             取消
           </button>
           <button
             className="send-button"
             onClick={handleSend}
             disabled={
               (contentType === 'text' && !content.trim()) ||
               (contentType === 'card' && !cardData) ||
               (contentType === 'image' && !imageUrl) ||
               (contentType === 'audio' && !audioUrl)
             }
           >
             发送留言
           </button>
         </div>
         
         {/* 卡片制作器 */}
         {showCardMaker && (
           <CardMaker
             onComplete={(card) => {
               setCardData(card);
               setContentType('card');
               setShowCardMaker(false);
             }}
             onCancel={() => setShowCardMaker(false)}
           />
         )}
         
         {/* 图片上传 */}
         {showImageUpload && (
           <ImageUpload
             maxSize={2 * 1024 * 1024} // 2MB
             onUpload={(url) => {
               setImageUrl(url);
               setContentType('image');
               setShowImageUpload(false);
             }}
             onCancel={() => setShowImageUpload(false)}
           />
         )}
         
         {/* 语音录制 */}
         {showAudioRecorder && (
           <AudioRecorder
             maxDuration={60} // 60秒
             onRecord={(url, duration) => {
               setAudioUrl(url);
               setContentType('audio');
               setShowAudioRecorder(false);
             }}
             onCancel={() => setShowAudioRecorder(false)}
           />
         )}
       </Modal>
     );
   };
   ```

   **CSS样式设计**:
   ```css
   .quick-message-modal .modal-content,
   .full-message-modal .modal-content {
     padding: 24px;
   }
   
   .greeting-text,
   .prompt-text {
     font-size: 16px;
     color: #333;
     margin-bottom: 20px;
     text-align: center;
   }
   
   .heartsphere-name {
     color: #FF9999;
     font-weight: 600;
   }
   
   .owner-name {
     color: #999;
     font-size: 14px;
   }
   
   .quick-templates {
     margin-bottom: 20px;
   }
   
   .template-label {
     font-size: 14px;
     color: #666;
     margin-bottom: 12px;
   }
   
   .template-list {
     display: flex;
     flex-direction: column;
     gap: 12px;
   }
   
   .template-item {
     padding: 16px;
     border: 2px solid #e0e0e0;
     border-radius: 12px;
     background: white;
     cursor: pointer;
     transition: all 0.2s;
     font-size: 15px;
     color: #333;
   }
   
   .template-item:hover {
     border-color: #FF9999;
     background: #FFF8F8;
     transform: translateY(-2px);
     box-shadow: 0 4px 12px rgba(255, 153, 153, 0.1);
   }
   
   .template-item.selected {
     border-color: #FF9999;
     background: #FFE5E5;
     box-shadow: 0 4px 12px rgba(255, 153, 153, 0.2);
   }
   
   .write-more-button {
     width: 100%;
     padding: 12px;
     border: 2px dashed #ccc;
     border-radius: 8px;
     background: white;
     color: #666;
     font-size: 14px;
     cursor: pointer;
     transition: all 0.2s;
   }
   
   .write-more-button:hover {
     border-color: #FF9999;
     color: #FF9999;
     background: #FFF8F8;
   }
   
   .message-input-area {
     margin-bottom: 20px;
     position: relative;
   }
   
   .message-textarea {
     width: 100%;
     padding: 16px;
     border: 2px solid #e0e0e0;
     border-radius: 12px;
     font-size: 15px;
     font-family: inherit;
     resize: vertical;
     min-height: 150px;
     outline: none;
     transition: border-color 0.2s;
   }
   
   .message-textarea:focus {
     border-color: #FF9999;
   }
   
   .message-textarea:disabled {
     background: #f5f5f5;
     cursor: not-allowed;
   }
   
   .char-count {
     position: absolute;
     bottom: 8px;
     right: 12px;
     font-size: 12px;
     color: #999;
   }
   
   .char-count.warning {
     color: #FF9999;
   }
   
   .content-type-selector {
     display: flex;
     gap: 12px;
     margin-bottom: 20px;
   }
   
   .type-button {
     flex: 1;
     padding: 12px;
     border: 2px solid #e0e0e0;
     border-radius: 8px;
     background: white;
     cursor: pointer;
     transition: all 0.2s;
     display: flex;
     flex-direction: column;
     align-items: center;
     gap: 4px;
     font-size: 14px;
     color: #666;
   }
   
   .type-button:hover {
     border-color: #FF9999;
     background: #FFF8F8;
   }
   
   .type-button.active {
     border-color: #FF9999;
     background: #FFE5E5;
     color: #FF9999;
   }
   
   .type-button span:first-child {
     font-size: 24px;
   }
   
   .card-preview,
   .image-preview,
   .audio-preview {
     margin-bottom: 20px;
     position: relative;
   }
   
   .image-preview img {
     width: 100%;
     max-height: 300px;
     object-fit: contain;
     border-radius: 8px;
   }
   
   .remove-image-button,
   .remove-audio-button {
     position: absolute;
     top: 8px;
     right: 8px;
     width: 32px;
     height: 32px;
     border-radius: 50%;
     background: rgba(0, 0, 0, 0.6);
     color: white;
     border: none;
     font-size: 20px;
     cursor: pointer;
     display: flex;
     align-items: center;
     justify-content: center;
   }
   
   .ai-suggestion {
     display: flex;
     gap: 8px;
     padding: 12px;
     background: #FFF8E1;
     border-left: 4px solid #FFD54F;
     border-radius: 8px;
     margin-bottom: 20px;
   }
   
   .suggestion-icon {
     font-size: 20px;
   }
   
   .suggestion-text {
     flex: 1;
     font-size: 14px;
     color: #666;
   }
   
   .modal-footer {
     display: flex;
     justify-content: flex-end;
     gap: 12px;
     padding: 16px 24px;
     border-top: 1px solid #f0f0f0;
   }
   
   .cancel-button,
   .send-button {
     padding: 10px 24px;
     border: none;
     border-radius: 8px;
     font-size: 14px;
     font-weight: 500;
     cursor: pointer;
     transition: all 0.2s;
   }
   
   .cancel-button {
     background: #f5f5f5;
     color: #666;
   }
   
   .cancel-button:hover {
     background: #e0e0e0;
   }
   
   .send-button {
     background: #FF9999;
     color: white;
   }
   
   .send-button:hover:not(:disabled) {
     background: #FF8888;
   }
   
   .send-button:disabled {
     background: #e0e0e0;
     color: #999;
     cursor: not-allowed;
   }
   ```

2. **留言模板系统**

   **快速模板**（3-5个，一键选择）：
   - ✨ "谢谢你分享这么美好的心域！"
   - 💙 "在这里找到了共鸣，谢谢！"
   - ⭐ "你的E-SOUL很特别，很喜欢！"
   - 🌟 "体验很棒，期待更多内容！"
   - 💚 "感谢你创造这个温暖的空间！"

   **完整模板库**（10-15个，分类展示）：
   - **感谢类**：3-4个模板
   - **赞美类**：3-4个模板
   - **情感类**：2-3个模板
   - **鼓励类**：2-3个模板
   - **分享类**：2-3个模板

   **AI个性化建议**（可选）：
   - 基于体验内容生成个性化留言建议
   - 提示可以提及的具体内容（如"你提到了[场景名称]，可以在这里表达感受"）

3. **留言卡片系统（推荐功能）**

   **卡片类型**：
   - **感谢卡片**：温暖的感谢主题，适合表达感谢
   - **赞美卡片**：精美的赞美主题，适合表达赞美
   - **祝福卡片**：温馨的祝福主题，适合表达祝福
   - **共鸣卡片**：深刻的情感主题，适合表达共鸣

   **卡片制作**：
   - 选择卡片模板
   - 输入留言文字
   - 选择卡片样式和颜色
   - 预览卡片效果

4. **留言交互优化**
   - **智能提示**：输入时给出智能提示（如"可以提及你最喜欢的E-SOUL"）
   - **字数提醒**：接近字数限制时给出友好提醒
   - **实时预览**：输入时可以实时预览留言效果
   - **草稿保存**：可以保存留言草稿，稍后继续编辑
   - **发送确认**：发送前显示确认对话框（可选）

### 3.2 留言展示和管理

#### 3.2.1 主人端留言展示

**功能描述**: 心域主人如何查看和管理收到的留言，让主人感受到温暖

**详细需求**:

1. **留言入口和通知**

   **留言入口**：
   - **个人中心入口**：在个人中心添加"收到的留言"入口
   - **心域管理入口**：在共享设置中显示留言数量
   - **通知提示**：有新留言时显示通知（数字徽章）

   **通知设计**：
   - **未读标识**：显示未读留言数量（红色数字徽章）
   - **通知提示**：有新留言时显示友好提示（如"有人给你的心域留言了！"）
   - **通知设置**：用户可以设置通知偏好

2. **留言列表界面**

   **列表布局**：
   ```
   ┌─────────────────────────────────────┐
   │  收到的留言 (15)            [筛选]  │
   ├─────────────────────────────────────┤
   │                                      │
   │  ┌──────────────────────────────┐  │
   │  │ [头像] 访客昵称   2小时前    │  │
   │  │                              │  │
   │  │ 谢谢你分享这么美好的心域！   │  │
   │  │ ✨💙⭐                        │  │
   │  │                              │  │
   │  │ [回复] [点赞] [删除]         │  │
   │  └──────────────────────────────┘  │
   │                                      │
   │  ┌──────────────────────────────┐  │
   │  │ [头像] 访客昵称   5小时前    │  │
   │  │                              │  │
   │  │ [卡片留言展示]                │  │
   │  │                              │  │
   │  │ [回复] [点赞] [删除]         │  │
   │  └──────────────────────────────┘  │
   │                                      │
   └─────────────────────────────────────┘
   ```

   **列表功能**：
   - **留言排序**：按时间倒序排列（最新的在前）
   - **筛选功能**：可以按类型筛选（全部/文本/卡片/图片/语音）
   - **搜索功能**：可以搜索留言内容（如果留言数量多）
   - **分页加载**：如果留言多，支持分页加载

3. **留言卡片设计**

   **卡片信息**：
   - **留言者信息**：显示留言者的头像、昵称
   - **留言内容**：显示留言的完整内容（文字、卡片、图片、语音）
   - **留言时间**：显示留言时间（友好格式，如"2小时前"、"昨天"、"3天前"）
   - **留言类型**：显示留言类型图标（文本/卡片/图片/语音）

   **卡片样式**：
   - **温馨设计**：使用温暖的卡片设计
   - **柔和配色**：使用柔和的配色和圆角
   - **层次感**：使用阴影和渐变增加层次感
   - **重点突出**：未读留言可以有不同的背景色或边框

4. **留言详情**

   **详情查看**：
   - **详情页面**：点击留言进入详情页面
   - **完整内容**：显示留言的完整内容
   - **留言者信息**：显示留言者的详细信息（如果允许）
   - **留言统计**：显示留言的查看次数、点赞数等

   **详情操作**：
   - **回复功能**：可以回复留言
   - **点赞功能**：可以为留言点赞
   - **收藏功能**：可以收藏留言（可选）
   - **删除功能**：可以删除不当留言
   - **分享功能**：可以分享留言（可选）

5. **留言统计和概览**

   **统计信息**：
   - **留言总数**：显示收到的留言总数
   - **未读数量**：显示未读留言数量
   - **最新留言**：显示最新留言的预览
   - **类型分布**：显示各类型留言的数量（可选）
   - **时间分布**：显示留言的时间分布（可选）

   **概览展示**：
   - **留言概览**：在个人中心显示留言概览
   - **最新留言**：显示最新3-5条留言的预览
   - **快速入口**：提供快速进入留言列表的入口

#### 3.2.2 留言展示样式

**功能描述**: 留言的视觉展示样式

**详细需求**:

1. **卡片样式**
   - **温馨设计**：使用温馨的卡片设计
   - **柔和文化**：使用柔和的配色和圆角
   - **层次感**：使用阴影和渐变增加层次感

2. **内容展示**
   - **文字排版**：清晰的文字排版，易读
   - **图片展示**：图片以卡片形式展示，支持放大
   - **语音播放**：语音以播放器形式展示

3. **交互效果**
   - **悬停效果**：鼠标悬停时有轻微放大
   - **点击反馈**：点击时有友好的反馈
   - **展开动画**：展开详情时的平滑动画

### 3.3 留言互动

#### 3.3.1 留言回复（可选）

**功能描述**: 主人可以回复留言，形成互动

**详细需求**:

1. **回复功能**
   - **回复按钮**：在留言卡片上显示回复按钮
   - **回复界面**：打开回复界面，可以输入回复
   - **回复发送**：发送回复，留言者会收到通知（如果支持通知）

2. **回复展示**
   - **回复列表**：在留言详情中显示所有回复
   - **回复标识**：清晰标识主人回复和留言者回复
   - **回复时间**：显示回复时间

#### 3.3.2 留言统计

**功能描述**: 统计和展示留言数据

**详细需求**:

1. **统计信息**
   - **总留言数**：统计收到的留言总数
   - **留言趋势**：显示留言数量趋势（如每周/每月）
   - **留言者统计**：统计留言者数量

2. **数据分析**（可选）
   - **热门留言**：显示最受欢迎的留言
   - **留言分类**：按类型分类统计留言
   - **情感分析**：分析留言的情感倾向（如果支持）

---

## 四、体验数据统计（可选）

### 4.1 体验摘要

**功能描述**: 为主人提供访问者的体验摘要（不包含详细内容）

**详细需求**:

1. **摘要内容**
   - **访问次数**：访问者访问的次数
   - **访问时长**：总访问时长
   - **访问场景**：访问了哪些场景
   - **对话统计**：与E-SOUL对话的轮数（不显示内容）

2. **隐私保护**
   - **不显示内容**：不显示具体的对话内容
   - **仅统计信息**：只提供统计数据
   - **用户同意**：需要访问者同意才能收集统计信息

3. **摘要展示**
   - **摘要列表**：在个人中心显示体验摘要列表
   - **摘要详情**：点击查看详细的统计数据
   - **数据可视化**：使用图表展示统计数据

### 4.2 访问日志

**功能描述**: 记录访问者的访问日志（可选）

**详细需求**:

1. **日志内容**
   - **访问时间**：记录访问时间
   - **访问场景**：记录访问了哪些场景
   - **停留时长**：记录在每个场景的停留时长
   - **离开时间**：记录离开时间

2. **隐私保护**
   - **匿名化处理**：不记录访问者的个人身份信息
   - **数据脱敏**：对敏感数据进行脱敏处理
   - **用户控制**：用户可以控制是否记录日志

---

## 五、技术实现方案

### 5.1 数据结构设计

```typescript
// 体验会话
interface ExperienceSession {
  id: string; // 会话ID
  shareConfigId: number; // 共享配置ID
  visitorId: number; // 访问者ID
  ownerId: number; // 心域主人ID
  startedAt: number; // 开始时间
  endedAt?: number; // 结束时间
  currentEraId?: number; // 当前场景ID
  // 体验数据（仅本地存储，不提交到服务器）
  localData: {
    conversations: any[]; // 对话记录
    visitedEras: number[]; // 访问的场景ID列表
    // ... 其他临时数据
  };
}

// 暖心留言
interface WarmMessage {
  id: number;
  shareConfigId: number; // 关联的共享配置
  visitorId: number; // 留言者ID
  ownerId: number; // 心域主人ID
  content: string; // 留言内容（文本）
  contentType: 'text' | 'image' | 'audio'; // 留言类型
  imageUrl?: string; // 图片URL（如果是图片留言）
  audioUrl?: string; // 语音URL（如果是语音留言）
  audioDuration?: number; // 语音时长（秒）
  isRead: boolean; // 是否已读
  createdAt: number; // 创建时间
  repliedAt?: number; // 回复时间（如果被回复）
  replyContent?: string; // 回复内容（如果被回复）
}

// 体验统计（可选）
interface ExperienceStats {
  id: number;
  shareConfigId: number;
  visitorId: number; // 匿名化或哈希后的访问者ID
  visitCount: number; // 访问次数
  totalDuration: number; // 总访问时长（秒）
  visitedEraIds: number[]; // 访问的场景ID列表
  conversationCount: number; // 对话轮数（不包含内容）
  lastVisitAt: number; // 最后访问时间
  createdAt: number;
}
```

### 5.2 API接口详细设计

#### 5.2.1 体验系统接口

**1. 开始体验**

**接口**: `POST /api/experience/start`

**请求体**:
```typescript
interface StartExperienceRequest {
  shareCode: string; // 共享码
}
```

**响应数据**:
```typescript
interface StartExperienceResponse {
  success: boolean;
  data: {
    sessionId: string; // 会话ID
    shareConfig: {
      id: number;
      userId: number;
      heartSphereName: string;
      ownerName: string;
      ownerAvatar?: string;
      shareType: 'all' | 'partial' | 'single';
      description?: string;
    };
    sharedEras: Array<{
      id: number;
      name: string;
      description?: string;
      thumbnailUrl?: string;
    }>; // 共享的场景列表
    permissions: {
      canChat: boolean; // 是否可以对话
      canSwitchEra: boolean; // 是否可以切换场景
      canViewContent: boolean; // 是否可以查看内容
    };
    expiresAt?: number; // 会话过期时间（如果有限制）
  };
}
```

**错误处理**:
- `400 Bad Request`: 共享码无效
- `403 Forbidden`: 无权限访问（连接未建立或被拒绝）
- `404 Not Found`: 共享配置不存在或已关闭

**2. 获取体验会话信息**

**接口**: `GET /api/experience/session/{sessionId}`

**路径参数**: `sessionId` - 会话ID

**响应数据**:
```typescript
interface GetExperienceSessionResponse {
  success: boolean;
  data: {
    sessionId: string;
    shareConfigId: number;
    visitorId: number;
    ownerId: number;
    startedAt: number;
    currentEraId?: number;
    permissions: {
      canChat: boolean;
      canSwitchEra: boolean;
      canViewContent: boolean;
    };
    isActive: boolean; // 会话是否仍然有效
    expiresAt?: number;
  };
}
```

**3. 结束体验**

**接口**: `POST /api/experience/end`

**请求体**:
```typescript
interface EndExperienceRequest {
  sessionId: string;
  leaveMessage?: string; // 可选：离开时的简短留言
}
```

**响应数据**:
```typescript
interface EndExperienceResponse {
  success: boolean;
  message: string;
  data?: {
    stats?: {
      duration: number; // 体验时长（秒）
      visitedEras: number; // 访问的场景数
      conversationRounds: number; // 对话轮数（仅统计，不包含内容）
    };
  };
}
```

#### 5.2.2 暖心留言接口

**4. 发送暖心留言**

**接口**: `POST /api/warm-messages`

**请求体**:
```typescript
interface SendWarmMessageRequest {
  shareConfigId: number; // 共享配置ID
  contentType: 'text' | 'card' | 'image' | 'audio';
  content: string; // 留言内容（文本）
  cardData?: {
    templateId?: string;
    title?: string;
    content: string;
    style?: any;
  }; // 卡片数据（如果contentType为'card'）
  imageUrl?: string; // 图片URL（如果contentType为'image'）
  audioUrl?: string; // 语音URL（如果contentType为'audio'）
  audioDuration?: number; // 语音时长（秒）
}
```

**响应数据**:
```typescript
interface SendWarmMessageResponse {
  success: boolean;
  data: {
    message: {
      id: number;
      shareConfigId: number;
      visitorId: number;
      ownerId: number;
      contentType: string;
      content: string;
      imageUrl?: string;
      audioUrl?: string;
      audioDuration?: number;
      cardData?: any;
      isRead: boolean;
      createdAt: number;
    };
    message: string;
  };
}
```

**5. 获取收到的留言列表（主人视角）**

**接口**: `GET /api/warm-messages`

**请求参数**:
```typescript
interface GetWarmMessagesParams {
  page?: number; // 页码（默认1）
  size?: number; // 每页数量（默认20，最大100）
  contentType?: 'text' | 'card' | 'image' | 'audio'; // 筛选留言类型
  unreadOnly?: boolean; // 只获取未读留言
  sortBy?: 'createdAt' | 'updatedAt'; // 排序方式
  sortOrder?: 'asc' | 'desc'; // 排序顺序
}
```

**响应数据**:
```typescript
interface GetWarmMessagesResponse {
  success: boolean;
  data: {
    messages: Array<{
      id: number;
      shareConfigId: number;
      visitor: {
        id: number;
        name: string;
        avatar?: string;
      };
      contentType: string;
      content: string;
      cardData?: any;
      imageUrl?: string;
      audioUrl?: string;
      audioDuration?: number;
      isRead: boolean;
      replyContent?: string;
      repliedAt?: number;
      createdAt: number;
      timeAgo: string; // 友好时间格式（如"2小时前"）
    }>;
    totalCount: number;
    unreadCount: number;
    page: number;
    size: number;
    hasMore: boolean;
  };
}
```

**6. 获取留言详情**

**接口**: `GET /api/warm-messages/{id}`

**路径参数**: `id` - 留言ID

**响应数据**:
```typescript
interface GetWarmMessageDetailResponse {
  success: boolean;
  data: {
    message: {
      id: number;
      shareConfigId: number;
      shareConfig: {
        id: number;
        heartSphereName: string;
      };
      visitor: {
        id: number;
        name: string;
        avatar?: string;
        bio?: string;
      };
      contentType: string;
      content: string;
      cardData?: any;
      imageUrl?: string;
      audioUrl?: string;
      audioDuration?: number;
      isRead: boolean;
      replyContent?: string;
      repliedAt?: number;
      createdAt: number;
    };
  };
}
```

**7. 标记留言为已读**

**接口**: `PUT /api/warm-messages/{id}/read`

**路径参数**: `id` - 留言ID

**响应数据**:
```typescript
interface MarkWarmMessageReadResponse {
  success: boolean;
  message: string;
}
```

**8. 批量标记已读**

**接口**: `PUT /api/warm-messages/read-all`

**请求体**:
```typescript
interface MarkAllWarmMessagesReadRequest {
  shareConfigId?: number; // 可选：只标记特定共享配置的留言
}
```

**响应数据**:
```typescript
interface MarkAllWarmMessagesReadResponse {
  success: boolean;
  data: {
    markedCount: number; // 标记的数量
  };
}
```

**9. 回复留言（可选）**

**接口**: `POST /api/warm-messages/{id}/reply`

**路径参数**: `id` - 留言ID

**请求体**:
```typescript
interface ReplyWarmMessageRequest {
  content: string; // 回复内容
}
```

**响应数据**:
```typescript
interface ReplyWarmMessageResponse {
  success: boolean;
  data: {
    message: {
      id: number;
      replyContent: string;
      repliedAt: number;
    };
    message: string;
  };
}
```

**10. 删除留言**

**接口**: `DELETE /api/warm-messages/{id}`

**路径参数**: `id` - 留言ID

**响应数据**:
```typescript
interface DeleteWarmMessageResponse {
  success: boolean;
  message: string;
}
```

**11. 点赞留言（可选）**

**接口**: `POST /api/warm-messages/{id}/like`

**路径参数**: `id` - 留言ID

**响应数据**:
```typescript
interface LikeWarmMessageResponse {
  success: boolean;
  data: {
    likeCount: number; // 更新后的点赞数
    isLiked: boolean; // 当前用户是否已点赞
  };
}
```

#### 5.2.3 体验统计接口（可选）

**12. 获取体验统计（主人视角）**

**接口**: `GET /api/experience/stats`

**请求参数**:
```typescript
interface GetExperienceStatsParams {
  shareConfigId?: number; // 筛选特定共享配置
  period?: 'week' | 'month' | 'year' | 'all'; // 时间周期
  startDate?: string; // 开始日期
  endDate?: string; // 结束日期
}
```

**响应数据**:
```typescript
interface GetExperienceStatsResponse {
  success: boolean;
  data: {
    period: string;
    statistics: {
      totalVisits: number; // 总访问次数
      uniqueVisitors: number; // 独立访问者数
      totalDuration: number; // 总访问时长（秒）
      averageDuration: number; // 平均访问时长（秒）
      mostVisitedEras: Array<{
        eraId: number;
        eraName: string;
        visitCount: number;
      }>; // 最受欢迎的场景
    };
    trends?: {
      date: string;
      visitCount: number;
      visitorCount: number;
    }[]; // 趋势数据（如果提供时间周期）
  };
}
```

#### 5.2.4 统一错误响应

所有接口使用统一的错误响应格式：
```typescript
interface ErrorResponse {
  success: false;
  error: {
    code: string;
    message: string;
    details?: any;
  };
  timestamp: number;
}
```

### 5.3 数据库设计

```sql
-- 体验会话表（可选，用于统计）
CREATE TABLE experience_sessions (
  id VARCHAR(100) PRIMARY KEY COMMENT '会话ID',
  share_config_id BIGINT NOT NULL COMMENT '共享配置ID',
  visitor_id BIGINT NOT NULL COMMENT '访问者ID',
  owner_id BIGINT NOT NULL COMMENT '心域主人ID',
  started_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '开始时间',
  ended_at DATETIME NULL COMMENT '结束时间',
  duration INT DEFAULT 0 COMMENT '体验时长（秒）',
  FOREIGN KEY (share_config_id) REFERENCES share_configs(id) ON DELETE CASCADE,
  FOREIGN KEY (visitor_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (owner_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_share_config (share_config_id),
  INDEX idx_visitor (visitor_id),
  INDEX idx_owner (owner_id)
);

-- 暖心留言表
CREATE TABLE warm_messages (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  share_config_id BIGINT NOT NULL COMMENT '共享配置ID',
  visitor_id BIGINT NOT NULL COMMENT '留言者ID',
  owner_id BIGINT NOT NULL COMMENT '心域主人ID',
  content TEXT NOT NULL COMMENT '留言内容',
  content_type VARCHAR(20) NOT NULL DEFAULT 'text' COMMENT '留言类型：text, image, audio',
  image_url VARCHAR(500) COMMENT '图片URL',
  audio_url VARCHAR(500) COMMENT '语音URL',
  audio_duration INT COMMENT '语音时长（秒）',
  is_read BOOLEAN DEFAULT FALSE COMMENT '是否已读',
  reply_content TEXT COMMENT '回复内容',
  replied_at DATETIME NULL COMMENT '回复时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (share_config_id) REFERENCES share_configs(id) ON DELETE CASCADE,
  FOREIGN KEY (visitor_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (owner_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_share_config (share_config_id),
  INDEX idx_owner_read (owner_id, is_read),
  INDEX idx_visitor (visitor_id)
);

-- 体验统计表（可选）
CREATE TABLE experience_stats (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  share_config_id BIGINT NOT NULL COMMENT '共享配置ID',
  visitor_hash VARCHAR(100) NOT NULL COMMENT '访问者哈希（匿名化）',
  owner_id BIGINT NOT NULL COMMENT '心域主人ID',
  visit_count INT DEFAULT 0 COMMENT '访问次数',
  total_duration INT DEFAULT 0 COMMENT '总访问时长（秒）',
  visited_era_ids TEXT COMMENT '访问的场景ID列表（JSON数组）',
  conversation_count INT DEFAULT 0 COMMENT '对话轮数',
  last_visit_at DATETIME NULL COMMENT '最后访问时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (share_config_id) REFERENCES share_configs(id) ON DELETE CASCADE,
  FOREIGN KEY (owner_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_share_config (share_config_id),
  INDEX idx_owner (owner_id),
  UNIQUE KEY uk_share_visitor_hash (share_config_id, visitor_hash)
);
```

---

## 六、实施计划

### 6.1 第一阶段：体验模式基础（1-2周）

- [ ] 实现体验模式标识和视觉设计
- [ ] 实现数据隔离机制
- [ ] 实现体验模式下的功能限制
- [ ] 实现体验流程（进入、体验、离开）

### 6.2 第二阶段：暖心留言系统（1-2周）

- [ ] 设计留言界面和交互
- [ ] 实现文本留言功能
- [ ] 实现图片留言功能（可选）
- [ ] 实现语音留言功能（可选）
- [ ] 实现留言展示和管理

### 6.3 第三阶段：留言互动（1周）

- [ ] 实现留言回复功能（可选）
- [ ] 实现留言统计功能
- [ ] 优化留言展示样式
- [ ] 实现留言通知（如果支持）

### 6.4 第四阶段：体验统计（1周，可选）

- [ ] 实现体验统计功能
- [ ] 实现访问日志记录（如果支持）
- [ ] 实现数据可视化
- [ ] 优化隐私保护机制

### 6.5 第五阶段：测试和优化（1周）

- [ ] 功能测试
- [ ] 性能测试
- [ ] 用户体验优化
- [ ] Bug修复

---

## 七、预期效果

1. **完整体验**：访问者能够完整体验心域的核心功能
2. **数据安全**：体验数据完全隔离，不影响主人的心域
3. **情感连接**：通过暖心留言增强访问者和主人之间的情感连接
4. **体验流畅**：体验过程流畅自然，没有割裂感

---

## 八、暖心留言建议

### 8.1 留言模板建议

1. **感谢类**
   - "谢谢你分享这么美好的心域，让我感受到了温暖！"
   - "在这里的体验让我很感动，谢谢你！"
   - "你的E-SOUL真的很特别，谢谢你的分享！"

2. **赞美类**
   - "你的心域设计得太棒了，每个细节都很用心！"
   - "这些E-SOUL都很有个性，很喜欢！"
   - "你的心域给了我很多启发，谢谢！"

3. **情感类**
   - "在这里找到了共鸣，谢谢你创造这个空间！"
   - "体验你的心域让我感到温暖和治愈！"
   - "希望我也能创造出这么美好的心域！"

### 8.2 留言互动建议

1. **主人回复**
   - 主人可以回复留言，表示感谢
   - 回复可以形成对话，增强连接

2. **留言展示**
   - 在个人中心展示收到的留言
   - 可以设置留言墙，展示精选留言

3. **留言纪念**
   - 可以给留言点赞
   - 可以收藏特别喜欢的留言
   - 可以生成留言纪念卡片（可选）

---

**文档完成**: 三个阶段的心域连接功能需求分析已完成

